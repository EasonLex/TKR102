# TDX 公車即時資料收集系統 — 開發日誌

> 記錄期間：2026-08-02 ~ 2026-08-04
> 專題目標：以雙北公車即時位置資料，推導「大眾運輸轉乘建議時間」

---

## 一、專題定義的思考過程

### 從問題出發，而非從工具出發

初期曾考慮多個方向（GitHub Archive、NYC TLC、事故熱點分析），最終收斂到 TDX 的判斷依據：

| 候選題目 | 資料特性 | 是否適合資料工程 |
|---|---|---|
| GitHub Archive | 靜態歷史檔，下載即完成 | 部分適合，缺少即時性挑戰 |
| NYC TLC | 官方 Parquet，已清洗 | 偏向查詢優化練習 |
| 事故熱點分析 | 靜態歷史檔 | ✗ 偏向資料分析／GIS |
| **TDX 公車即時位置** | **持續變動、不可回補** | **✓** |

**關鍵判準：資料會不會動。**

靜態資料不會產生 backfill、schema 漂移、斷線補救、去重等問題，
而這些正是資料工程的核心工作。事故資料最終被重新定位為「維度表」，
用來解釋公車誤點成因，而非獨立主題。

### 粒度推導鏈

```
問題：轉乘建議時間應該抓幾分鐘？
  ↓
拆解：前車實際到站時間 + 站內步行時間 + 後車實際發車時間
  ↓
發現：答案不是平均值，而是分布（需要說「P90 需要 11 分鐘」）
  ↓
推論：同一轉乘情境需被觀測「夠多次」才能估計分布
  ↓
結論：資料量需求是被問題本身逼出來的，不是硬湊的
```

### 站內步行時間 — 無公開資料的處理

此為專題最大的資料缺口，採取的策略：

1. 用 GIS 座標計算直線距離 ÷ 步行速度（1.2–1.4 m/s）打底
2. 從觀測資料的分布下緣反推步行時間下限
3. 在報告中明確揭露為主要誤差來源

**判斷依據**：目標是「建議時間」而非「精確步行時間」。
對一個 P90 為 11 分鐘的建議值，1 分鐘誤差影響有限，不應在此過度投入。

---

## 二、問題與解決紀錄

### P1. 認證失敗 — `invalid_client`（第一次）

```json
{"error":"invalid_client","error_description":"Invalid client credentials"}
```

**排查過程**：先用 curl 驗證請求格式（Content-Type、參數位置、grant_type 拼寫），
確認格式無誤後轉向帳號端。

**根因**：從網頁複製 Client ID 時漏掉尾段（UUID 格式較長，手動拖曳選取容易截斷）。

**解法**：使用複製按鈕重新取得，並刪除重建金鑰。

**學到**：認證失敗的錯誤訊息通常不精確，`invalid_client` 不會指出實際錯在哪。
排錯順序應為「先確認請求格式 → 再確認憑證內容 → 最後確認帳號狀態」。

---

### P2. 憑證外洩

在對話中完整貼出 client_secret。

**解法**：立即刪除該組金鑰並重建。

**學到**：憑證不進聊天視窗、不進 git、不寫死在程式碼。
專案自始採用環境變數管理（`os.environ`），後續驗證 git 歷史時確認未外洩。

---

### P3. 城市代碼錯誤 — HTTP 400

```
.../City/New%20Taipei?%24format=JSON
```

**根因**：城市代碼誤寫為 `"New Taipei"`，正確為 `NewTaipei`（無空白）。

**學到**：**4xx 是「你送錯了」，5xx／timeout 才是「對方的問題」。**
這個分界決定了重試策略——對 4xx 重試一萬次都不會成功，只會浪費限流額度。
延伸原則：4xx 應該「吵」（讓人知道要修），5xx 應該「安靜重試」。

---

### P4. 限流 429 — 且失敗具系統性偏誤

```
FAILED HTTPError: 429 Too Many Requests ... City/NewTaipei
```

**根因**：TDX 導入會員訂閱機制後，基礎會員限制為 **5 次/分/金鑰**。
原設計每 20 秒打 2 個城市 = 6 次/分，略微超標。

**更重要的發現**：失敗幾乎全部集中在 NewTaipei。
原因是每輪先打 Taipei 再打 NewTaipei，額度用盡時永遠是後者被擋。

> **這是本次專題最關鍵的一個發現。**
> 隨機缺漏可以忍受，**系統性缺漏會讓結論失真**。
> 若未察覺而持續收集，最終會得到「新北資料比台北少 15%」的資料集，
> 而在做跨市轉乘分析時完全不會察覺。

**解法**：
1. 從「連打兩次後睡 20 秒」改為「交錯輪詢」，每 N 秒打一次、城市輪流
2. 加入針對 429／5xx 的指數退避（不對其他 4xx 退避）
3. 後續升級訂閱方案至 5 次/秒

**曾考慮但拒絕的方案**：建立 3 把金鑰輪流使用以繞過限流。
理由是這違反服務精神，且在履歷上「評估用量後申請對應方案」遠比「繞過限制」專業。

---

### P5. 交錯輪詢 vs 分開 process — 設計取捨

決定採用「單一 process 交錯輪詢」而非「每城市獨立 process」，理由：

| 面向 | 交錯輪詢 | 獨立 process |
|---|---|---|
| 取樣相位差 | **固定 5 秒，偏誤可預測** | 隨機（取決於啟動時間） |
| 故障隔離 | 較弱 | 較強 |
| 實作複雜度 | 低 | 需處理共享 token、限流計數 |

**關鍵認知**：偏誤處理的優先順序是
**① 消除它 → ② 讓它變成固定的 → ③ 事後校正**。
交錯輪詢已達成第 ①②層，分 process 反而會退回「未知相位」。

分 process 解決的是**故障隔離與擴展性**，不是資料品質——
這兩件事不應混為一談。

---

### P6. 取樣頻率的決定

升級訂閱後額度變為 5 次/秒（放大 60 倍），
但**未將取樣加密到極限**。

**原因**：TDX 資料約 20 秒更新一次。若每 2 秒抓一次，
會得到十份 GPSTime 完全相同的資料——十倍儲存成本、零額外資訊。

> **取樣頻率的上限應由「來源更新頻率」決定，不是由「額度」決定。**

**最終設定**：`INTERVAL = 5`，兩城市輪流 → 各城市 10 秒一輪。
即 2 倍過取樣，確保不因來源偶爾提早更新而漏抓。
實測去重比例 0.575，證實設定合理（未過度取樣）。

多出的額度規劃用於**增加維度**（EstimatedTimeOfArrival、RealTimeNearStop、
捷運即時資料），而非加密同一支 API。

---

### P7. 環境類問題

| 錯誤 | 根因 | 解法 |
|---|---|---|
| `Import "requests" could not be resolved` | 未安裝套件 | 建立 venv + 安裝，後改用 uv |
| `NameError: TDX_CLIENT_ID is not defined` | `os.environ[TDX_CLIENT_ID]` 少了引號 | 補上引號 |
| `exit 127` | Mac 上只有 `python3`，無 `python` | 使用 venv／uv run |
| `exit 2` | `uv run` 在無 `pyproject.toml` 的目錄執行 | 切換至正確目錄 |
| `IndexError: sys.argv[1]` | VS Code Run 按鈕不帶參數 | 改用終端機執行 |

**學到**：
- **看錯誤「類型」比看錯誤「訊息」重要。**
  `NameError` 與 `KeyError` 指向完全不同的問題——若只看變數名，
  會誤往環境變數方向查而浪費時間。
- **相對路徑是相對於「執行位置」，不是「檔案位置」。**
  後續改為 `argparse` 傳入資料路徑，讓程式不對「資料在哪」做任何假設。
- 判準：**如果換個環境就要改程式碼，那它就該是設定。**

---

### P8. 沒有 log 就沒有現場

初期使用 VS Code 的 Run 按鈕執行，未保留 log，導致 `exit 127` 只能靠猜測排查。

**解法**：
1. 將 log 寫檔邏輯直接寫進程式（不依賴 shell 導向，避免「忘記加參數」）
2. `print(..., flush=True)` — Python stdout 導向檔案時有緩衝，
   不 flush 會導致 `tail -f` 看不到內容，且程式被中斷時最後一段 log 直接消失
3. 保留 `stdout.log` 捕捉啟動階段的錯誤（那時 log 函式尚未生效）
4. 記錄 HTTP 狀態碼而非僅記錄例外類型——狀態碼是判斷「該不該重試」的唯一依據

**對照效果**：`exit 127` 靠猜、`exit 2` 三十秒內從 log 找到答案。

---

## 三、Collector 的關鍵設計

| 設計 | 防的是什麼 |
|---|---|
| Token 快取保留 **60 秒**緩衝 | 卡在過期邊界時隨機噴 401（每天僅一兩次，最難察覺） |
| `flush=True` | log 緩衝導致關鍵訊息遺失 |
| 每圈都呼叫 `makedirs(exist_ok=True)` | 跨日時資料夾不存在，程式只能活到當天午夜 |
| `except Exception` 包住單一城市 | 成本不對稱：漏抓一輪損失 20 秒，程式死掉損失整晚 |
| `sleep(INTERVAL - elapsed)` | 避免實際間隔漂移，且尖峰時段（請求變慢）取樣變疏 |
| `max(0, ...)` | 對方變慢時 `sleep(負數)` 會拋例外殺死程式 |
| 存原始 `.json.gz`，**完全不解析** | 粒度尚未確定，存解析後的結果一旦錯誤即無法回復 |

> **核心原則：原始資料永遠原封不動存一份，所有清洗都從它重算。**
> 清洗邏輯一定會寫錯；錯了能重跑就沒事，但邊抓邊清就沒救。

---

## 四、資料品質檢查結果（2026-08-03，NewTaipei，6.5 小時）

```
原始筆數        : 2,641,690
去重後筆數      : 1,519,796
去重比例        : 0.575
不重複車輛數    : 2,245
不重複路線數    : 458

端到端延遲(秒)  : 中位數 12.0 / P95 24.0 / 最大 604.0
快照筆數        : 最少 1112 / 中位數 1264 / 最多 1609
空快照數        : 0

離群座標筆數    : 310 (0.012%)，涉及 7 台車
  EAL-3990: 302 筆  ← 佔異常的 97%
  其餘 6 台各 1-2 筆
```

### 發現 1：GPS 無效座標 (0, 0)

`Lat: 0.4239 / Lon: 0.0000` — 幾內亞灣外海，GPS 未定位時的預設值。

**診斷**：非普遍性定位失敗，而是**單一車機故障**（EAL-3990 佔 97%），
其餘為隧道／地下道造成的瞬時失準。

**處理決策**：採用**座標範圍規則**而非車牌黑名單。
黑名單需人工維護，故障車輛會更換，這種機制在自動化 pipeline 中遲早過期；
範圍檢查對新出現的故障車自動生效。

**關鍵原則：標記而非刪除。**
在清洗層加上 `is_valid_position` 標記，原始層保留全部資料。理由：
- 「哪些車機常故障」本身是有價值的資訊
- 若範圍設定錯誤（例如確有路線跑到基隆），刪除後無法回復

**危險性**：這類資料不會報錯，看起來完全正常。
若未察覺，計算「車輛與站牌距離」時會產生 2,700 公里的數值並靜靜混入統計。

### 發現 2：端到端延遲最大 604 秒

中位數 12 秒、P95 24 秒屬健康，但最大值達 10 分鐘。

對轉乘時間計算構成實質威脅——需訂定門檻並標記超時資料。
**這正是保留三個時間戳（GPSTime／SrcUpdateTime／UpdateTime）的價值。**

### 發現 3：狀態碼雜訊約 15%

- `DutyStatus: 2` 佔 15%（非營運狀態）
- `BusStatus: 99` 佔 3.5%（未知／異常）

這些位置資料為真，但對「轉乘時間」分析屬雜訊。
**在分析層過濾，不在 collector 端過濾。**

### 發現 4：整點階梯跳升（真實現象，非 bug）

視覺化後發現 14:00／15:00／16:00／17:00 各有一次陡升，
對應公路客運的整點發車班表。

意義：證明資料捕捉得到真實營運節奏；同時代表
**「整點效應」是後續誤點分析必須控制的變數**。

### 發現 5：折線圖會掩蓋資料破洞

13:05–13:50 約 45 分鐘無資料，但 matplotlib 將兩端點連成一條
看起來很合理的直線。

**修正**：在缺口處插入 `NaN` 使線條斷開。
**通則**：不完整的時間區間（如僅有半小時的 17 點）在長條圖中
會與完整小時等高，是報表最常見的誤導來源。

---

---

## 四之二、雙城市對照與 BusStatus 分析（2026-08-04）

將體檢腳本擴充為同時處理兩個城市後，跨城市比較立刻暴露出單一城市看不出來的問題。

### 對照表（2026-08-03，同一時段 6.5 小時）

| 指標 | Taipei | NewTaipei |
|---|---|---|
| 原始筆數 | 3,324,159 | 2,641,690 |
| 去重比例 | 0.583 | 0.575 |
| 不重複車輛 | 2,735 | 2,245 |
| 不重複路線 | 352 | 458 |
| 離群座標 | **4** | **310** |
| `BusStatus` 異常 | **542** | **1** |
| 空值總數 | 0 | 447 |

> **單一城市的資料看起來都「正常」，是對照之後才看出異常。**
> 這是後續所有品質檢查都應同時涵蓋兩個城市的理由。

### 發現 6：`BusStatus` 的填寫完整度依業者而異，不可作為判斷依據

官方代碼定義：

```
0:正常  1:車禍  2:故障  3:塞車  4:緊急求援  5:加油
90:不明  91:去回不明  98:偏移路線  99:非營運狀態
100:客滿  101:包車出租  255:未知
```

實測結果：

- 台北 542 筆異常**全部是 `2`（故障）**，新北僅 1 筆
- **`3`（塞車）與 `100`（客滿）在兩個城市皆為 0**，從未被回報

**推論**：新北並非沒有故障，而是業者不填這個欄位。

**兩個實務影響**：

1. **不能用 `BusStatus` 過濾資料。**
   若在台北過濾掉故障車、在新北過濾不掉，會造成兩市資料品質不對等
   ——又是一個系統性偏誤。

2. **不能用 `BusStatus` 偵測壅塞。**
   原本期待用 `3`（塞車）判斷路段壅塞，但該欄位從未被填寫。
   壅塞必須自行從位置資料推導（連續多筆位置未移動、站間旅行時間異常拉長）。

> **「發現某個欄位永遠是空的」，其價值不亞於發現異常值。**
> 這類發現只能靠實際檢視資料得到，讀 API 文件看不出來。

**設計原則**：所有結論都必須能從最原始的觀測（位置 + 時間）推導出來。
衍生欄位（業者自填的狀態碼）的品質不受你控制，只能當輔助訊號，不能當判斷依據。

### 發現 7：異常的形態是「少數車輛、長時間」，不是隨機雜訊

歸納三個獨立的異常觀察：

| 異常類型 | 城市 | 筆數 | 涉及車輛 | 形態 |
|---|---|---|---|---|
| GPS 座標 (0,0) | NewTaipei | 310 | 7（EAL-3990 佔 302） | 單車長時間 |
| `BusStatus=2` 故障 | Taipei | 542 | **2**（EAL-0016 佔 377） | 單車長時間 |
| 其餘離群座標 | 兩市 | 個位數 | 各 1–2 筆 | 瞬時失準 |

EAL-0016 的 377 筆 ≈ 約 1 小時持續回報故障狀態，期間仍在傳送位置資料。

> **結論：資料異常幾乎都是「單一車輛長時間異常」，而非隨機散布的雜訊。**

**對清洗策略的影響**：應**以車輛為單位判斷**（這台車今天是否正常），
而非逐筆過濾。逐筆過濾會留下「故障車停在路邊一小時」這種
**看起來合理、實際上不該納入**的觀測——它不會觸發任何異常值檢查，
但會嚴重汙染「站間旅行時間」的統計。

**具體規則方向**：一台車連續 N 分鐘位置無實質移動即標記為異常，
不論其 `BusStatus` 填什麼。此規則對兩個城市一視同仁，
不依賴業者的填寫習慣。

### 指標設計上的修正

| 問題 | 說明 |
|---|---|
| `null_total` 算錯 | 原本是 `len(nulls)`（有幾個欄位有空值），應為 `df.isna().sum().sum()`。**名稱與內容不符的指標比沒有指標更糟。** |
| `bad_count` 只在異常時寫入 | 指標必須每天都有，即使是 0；否則時間序列會有洞 |
| 先建桶子才看內容 | `bus_status_abnormal` 這個分類建立在「五個代碼都該排除」的假設上，但實際全是同一個代碼。**聚合會丟失資訊，且丟掉後就看不見了** |

**通則：每個異常筆數指標都應配一個「涉及車輛數」。**
單看筆數無法分辨「一台車壞很久」與「很多台車偶爾壞」，
而這兩者的處理方式完全不同。

```python
metrics["bad_count"] = int(len(bad))
metrics["bad_vehicles"] = int(bad["PlateNumb"].nunique())
```

### 探索與生產的分工

此階段開始引入 Jupyter notebook 做資料探索。

**分工原則**：
- **notebook** — 探索。資料載入一次留在記憶體，反覆換角度檢視
- **`.py` 腳本** — 穩定下來的邏輯

理由：notebook 的格子可任意順序執行，結果可能依賴某個已忘記執行過的格子，
這類 bug 極難重現。故驗證完的邏輯一律搬進腳本。

（`.ipynb` 會儲存執行結果含資料內容，需加入 `.gitignore`。）

---

---

## 四之三、第一次生產事故：磁碟空間耗盡（2026-08-04）

### 事故經過

例行檢查 `grep -c FAILED collector.log` 發現 29 次失敗，追查後：

```
[2026-08-04T22:58:26+08:00] Taipei: FAILED OSError: [Errno 28]
  No space left on device: 'raw/Taipei/2026-08-04/225826.json.gz'
```

**Mac mini 硬碟寫滿，collector 持續執行但寫檔全數失敗。**

當日檔案數 8,218 / 預期 8,640 = **95.1% 完整**，缺約 70 分鐘的快照。

### 這次事故與先前所有問題的本質差異

| | 先前的問題 | 這次 |
|---|---|---|
| 症狀 | 程式跑不起來 | **程式跑得好好的** |
| 察覺方式 | 立刻看到錯誤 | 主動翻 log 才發現 |
| 損失 | 無（尚未開始收集） | **70 分鐘資料，不可回補** |

> **這是第一個「已經正常運作的東西悄悄壞掉」的案例。**
> 沒有任何機制通知，是例行檢查才發現的——
> 這正是監控存在的理由。

更危險的是它**時好時壞**：系統偶爾釋放空間，寫入又會短暫成功。
造成的資料破洞不是連續區段，而是零散分布，
事後極難分辨「這時段沒車」與「這時段寫不進去」。

### 根因：Collector 對兩類失敗一視同仁

原本的設計是「任何例外都記 log 繼續跑」，這在起步階段是正確的
（見〈Collector 的關鍵設計〉：活著比正確重要）。

但磁碟滿與 API 失敗的性質完全不同：

| 失敗類型 | 會自己好嗎 | 正確反應 |
|---|---|---|
| API timeout／5xx | 會 | 安靜重試 |
| 429 限流 | 會 | 退避後重試 |
| **磁碟滿** | **不會** | **必須告警** |

> **「重試能解決的」與「重試不能解決的」需要不同的處理。**
> 對後者持續重試，只會不斷產生失敗記錄而毫無進展。

這與 P3 學到的「4xx 該吵、5xx 該安靜重試」是同一個原則的延伸。

### 處置

**立即**：加入磁碟空間哨兵

```python
import shutil
free_gb = shutil.disk_usage(".").free / 1e9
if free_gb < 10:
    log(f"DISK LOW: {free_gb:.1f} GB")
```

**容量推估**：

| 項目 | 數值 |
|---|---|
| 實測用量 | 約 500 MB/天（雙北合計，JSON.gz） |
| 剩餘空間 | 29 GB |
| 可撐天數 | **約 30 天** |
| 距結課所需 | 60 天以上 |

**結論：現有空間不足以撐到專題結束。**

**本週**：實作 gz → Parquet 轉檔

- 預期由 500 MB/天 降至 150–250 MB/天
- 流程：轉檔 → **驗證筆數一致** → 刪除 gz
- 驗證步驟不可省略：這是專案中第一次執行不可逆的刪除

> **這是「讓痛感出現再選工具」的實例。**
> Parquet 原本被規劃在「中期」，是被硬碟容量逼到眼前的，
> 不是因為它聽起來專業。

### 附帶發現：檔案系統大小寫敏感性

`ls raw/taipei`（小寫）在 macOS 上可以成功，因為 APFS 預設不分大小寫。
**Linux 會分。** 這是日後上雲的常見踩雷點。

---

## 四之四、版控問題：產出檔不該進版控

### 症狀

Mac mini 上執行 `git pull` 前，`git status` 顯示：

```
modified: Chart/daily_report.txt
modified: Chart/metrics.jsonl
modified: Chart/inspect_*.png
```

這些是體檢腳本的**產出**，在 Mac mini 上執行時被覆蓋，因而擋住 pull。

### 根因

程式碼與產出檔放在同一個資料夾（`Chart/`），且產出檔被納入版控。

**每次在生產機執行報告，都會製造一次 git 衝突。**

### 處置

```bash
git rm --cached Chart/daily_report.txt Chart/metrics.jsonl Chart/*.png
```

`.gitignore` 補上：
```
.DS_Store
Chart/*.png
Chart/daily_report.txt
Chart/metrics.jsonl
```

### 修正一個先前的錯誤判斷

初期曾判斷「`metrics.jsonl` 體積小、能證明 pipeline 持續運作，應納入版控」。

**這個判斷是錯的**，因為它忽略了一件事：**兩台機器都會寫這個檔案。**

> **產出檔應該只有一個權威來源。**
> 兩邊都能寫的檔案遲早會分岔，而 git 無法自動合併它們。

正確做法：Mac mini 作為唯一產生者，需要時以 `scp` 取回，
或後續直接寫入資料庫。

### 延伸原則

**程式碼與產出混在同一目錄，是這次問題的根源。**
後續應將所有產出統一寫入 `output/`，整個資料夾納入 `.gitignore`。

### 另一個關鍵操作紀錄

`git pull` 只更新檔案，**正在執行的 process 仍是舊程式碼**。
部署新版本必須重啟服務：

```bash
launchctl bootout gui/$(id -u)/com.ethan.tdxcollector
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.ethan.tdxcollector.plist
```

> **「程式碼更新了」與「跑著的東西更新了」是兩件事。**
> 這是實務上常見的事故來源，在 Docker／雲端環境中會更隱蔽
> （映像檔未重建、容器未重啟）。

---

---

## 四之五、儲存格式轉換：gz → Parquet（2026-08-05）

### 動機不是「Parquet 比較專業」，是兩個具體的痛

**痛點一：空間**（承四之三）
JSON.gz 用量 750 MB/天，剩餘 26 GB 只能撐 34 天，距結課還需 60 天以上。

**痛點二：讀取速度**
執行 `inspect_day.py` 處理 8/4 整天（8,423 個檔）時卡死。
原因是 `rows.extend()` 累積約 500 萬筆後一次 `json_normalize`，
記憶體膨脹且極慢——而 8/3 只有 2,035 個檔（半天）時毫無問題。

> **原始層方便保存，但不適合反覆查詢。**
> 每次分析都重新解析 8,423 個 JSON 檔，這個成本會一直付。

**附帶教訓**：在生產機上跑吃資源的分析，會與 collector 搶記憶體。
若觸發 swap，可能連帶拖垮資料收集——
**分析跑不完是小事，資料收集中斷是大事。**

### 成果

| 格式 | 大小 | 相對 |
|---|---|---|
| JSON.gz | 390 MB（8/3 Taipei，332 萬筆）| 100% |
| Parquet + zstd | **56 MB** | **-86%** |

推估整天約 110 MB/城市，可用天數由 34 天延長至 **230 天以上**。

### 驗證結果（8/3 Taipei）

| 指標 | metrics.jsonl | Parquet | |
|---|---|---|---|
| 原始筆數 | 3,324,159 | 3,324,159 | ✓ |
| 去重後筆數 | 1,938,030 | 1,938,030 | ✓ |
| 不重複車輛 | 2,735 | 2,735 | ✓ |
| 不重複路線 | 352 | 352 | ✓ |
| GPS 時間範圍 | 11:01:15–17:27:09 | 同（轉台北後）| ✓ |

**驗證強度的思考**：

| 強度 | 方法 | 能抓到什麼 |
|---|---|---|
| 弱 | 筆數、欄位數 | 整批遺失 |
| 中 | `sum()`／`max()` | 單一極端值改變（`median`／`p95` 是穩健統計量，反而不敏感）|
| 強 | 排序後 hash | 任何內容改變 |

**但 hash 不可能位元相同**——轉檔本身就會改變表示方式（時間轉型別、欄位改名）。
要驗的是「語意等價」，不是「位元相同」，故 hash 需在**轉換後的表示**上計算。

### 三個被 schema 攔下來的型別錯誤

第一版未定義 schema，讓 pandas 自行推斷，產生了一個**沒有任何檢查會發現的資料損毀**：

```
原始 JSON: "OperatorNo": "0303"   ← 字串
推斷結果:  operator_no = 303      ← int64，前導零永久消失
```

> **這是「靜靜寫錯」的教科書案例，且躲過了所有既定驗證。**
> 筆數一致、欄位齊全、hash 也會通過——因為 hash 在轉換後計算，兩邊錯得一模一樣。

改為明確定義 SCHEMA 後，三個問題立刻被攔下：

| 錯誤 | 根因 | 處置 |
|---|---|---|
| `Expected string, got int64` (operator_id) | `read_json` 讀檔當下即推斷成 int | 在 `read_json` 指定 `dtype={...: str}` |
| `Integer 65535 not in range` (azimuth) | int8/int16 容納不下 | 型別放寬為 int32，**保留原值不清洗** |
| 前導零遺失 | 同第一項 | 同上 |

> **關鍵教訓：型別必須在資料進入系統的第一刻就固定住。**
> 中間任何一個環節做了推斷，資訊就已遺失——
> 後續的 schema 只能保證「型別是字串」，不能保證「內容正確」。

**每一個報錯都是省下來的除錯時間。** 若無 schema，這三個問題會全數靜靜通過，
直到分析階段算出詭異結果才回頭追查，屆時成本高出數十倍。

### 設計決策紀錄

| 項目 | 決定 | 理由 |
|---|---|---|
| 分區單位 | 一天一城市一檔 | 與既有目錄結構一致，約 110 MB 便於處理 |
| 壓縮 | zstd | 空間與速度的折衷（snappy 快但大、gzip 小但慢）|
| 時間 | 存 UTC，讀取時轉台北 | 時區處理集中在一處 |
| **營運日** | **用台北時間算，由呼叫端傳入** | UTC 的 8/4 16:00 是台北 8/5 00:00，分區日期須以營運日為準，不從資料內容推斷 |
| 識別碼 | 一律 string | 不會做算術的欄位都是識別碼 |
| GeoHash | 保留不重算 | 它是原始資料的一部分；若 TDX 改變演算法，自行重算的會對不上 |
| 巢狀欄位 | 攤平 + snake_case | 帶點的欄位名在部分查詢引擎需加引號 |
| 去重 | **不在轉檔時做** | 原始層保持原始，去重是分析層的職責 |

補上 `city` 與 `data_date` 兩個分區欄位——
成本極低（單一值壓縮後幾乎不佔空間），但避免多城市合併讀取時無法區分來源。

### Schema 漂移偵測

轉換層比對上游實際欄位與預期欄位，差異即告警。

預設 `strict=False`（警告後繼續）而非直接拋錯：
**轉檔中途因上游多一個欄位就整批失敗，代價過高。**
但此彈性僅適用於轉檔階段——合併階段的失敗必須中止並拋出，
否則會產出「缺資料但成功結束」的 Parquet（見原則 15）。

---

## 四之六、異常值的兩層分類，以及一次被推翻的判斷

### 起點：speed 出現 361 kph

公車不可能時速 361 公里。檢視分布後發現：

```
619-U5: 32 筆 / EAL-0080: 10 / 558-U5: 5 / EAL-3661: 4 / KKA-2786: 4
```

**這打破了先前歸納的模式。** 前三個異常都是「少數車輛、數百筆」，
這次是「多數車輛、個位數筆」。

**成因不同**：不是車機故障，而是 GPS 定位跳動造成的瞬時假速度
——車機用兩個相隔過遠的定位點除以時間，算出物理上不可能的數值。
這是**計算誤差，不是設備故障**。

### 修正：清洗策略需要兩層，不是一層

〈發現 7〉曾歸納「應以車輛為單位判斷」。這個結論**不完整**：

| 層級 | 判斷對象 | 適用異常 |
|---|---|---|
| **車輛層** | 這台車今天是否整體不可信 | GPS (0,0)、持續故障 |
| **紀錄層** | 單筆觀測是否物理合理 | 速度超過上限、位置瞬移 |

**兩層都需要，因為異常有兩種成因。**

### 一次被推翻的判斷：azimuth 65535

`azimuth = 65535`（= 2^16 - 1）共 6,796 筆。三個特徵都指向「哨兵值」：

1. 恰好是 unsigned 16-bit 最大值
2. 遠超合理範圍（方位角應為 0–359）
3. 集中在少數車輛（EAA-198 326 筆、367-U8 263 筆⋯⋯）

**初步判斷：車輛層異常，方位感測器故障。**

但注意到一個矛盾：前五台車合計僅 1,203 筆，總數卻是 6,796
——**尾巴很長**，與 GPS (0,0) 的「一台車佔 97%」形態不同。

於是提出假設並以另一個欄位交叉驗證：

```python
df[df["azimuth"] == 65535]["speed"].value_counts()

speed 0 → 4,088 筆   (60%)
      1 →   801
      2 →   465
      3 →   302
      4 →   204
```

**假設證實：車速趨近於零時無法判斷行進方向。**

> **65535 不是異常，是語意值——它表示「方位未定義」。**

### 差點犯下的錯誤

若將 65535 當作異常清除，會**系統性地移除所有低速與靜止的車輛**。

而**靜止正是判斷「到站」的核心訊號**——這會直接摧毀專題的核心分析，
且清洗後的資料看起來會非常「乾淨」，完全不會觸發任何品質檢查。

### 最終分類

| 異常 | 真實性質 | 處理 |
|---|---|---|
| GPS 座標 (0,0) | 定位失敗 | 車輛層排除 |
| `BusStatus = 2` | 車輛故障 | 標記，不強制排除 |
| `azimuth = 65535` | **語意值：方位未定義** | **保留，不清洗** |
| `speed > 100` | GPS 跳動計算誤差 | 紀錄層標記 |

### 本節的核心教訓

> **「看起來像異常」與「是異常」是兩件事，必須用另一個獨立欄位交叉驗證。**

推理鏈：發現異常 → 觀察分布形態 → 提出假設 → 用獨立欄位驗證 → **推翻原判斷**。

三個特徵同時指向「異常」的情況下仍然是誤判——
這說明**特徵吻合不等於結論成立**，尤其當誤判的代價是摧毀核心分析時。

### 待確認事項

`azimuth 65535` 中集中出現的前五台車（EAA-198 等），
需確認其是否幾乎全對應 `speed = 0`（長時間停靠或壅塞），
以完全排除「部分車輛確實感測器故障」的可能。

---

---

## 四之七、專案重構與路徑統一（2026-08-06）

### 動機：同一類問題出現了五次

路徑錯誤反覆發生：

| 次序 | 症狀 |
|---|---|
| 1 | `inspect_day.py` 在 MacBook 上找不到檔案（`../Collection/raw`）|
| 2 | 同一支腳本在 Mac mini 上找不到（目錄結構不同）|
| 3 | 新寫的 `convert_to_parquet.py` 又踩一次 |
| 4 | `Path.name` 誤用（`.name` 只回傳最後一段，不是完整路徑）|
| 5 | 兩支腳本的 `PROJECT_ROOT` 層數不一致（`.parent` vs `.parent.parent`）|

**根因**：每支腳本各自計算路徑，且混用相對路徑。

> **判準：如果換個環境就要改程式碼，那它就該是設定。**
> 反覆出現的同一類錯誤，代表需要的是結構性修正，不是逐次修補。

### 重構後的結構

```
TrafficProject/
├── pyproject.toml
├── raw/                    ← 資料留在根目錄
├── output/
│   ├── parquet/
│   ├── reports/
│   └── logs/
└── src/trafficproject/     ← 程式碼集中
    ├── paths.py            ← 唯一的路徑來源
    ├── collector.py
    ├── transform.py
    ├── convert_to_parquet.py
    └── inspect_day.py
```

**程式碼進 `src/`，資料與產出留在根目錄。**
資料不該跟著程式碼走——這個分離讓 repo 只含程式碼，
也讓日後切換儲存位置（例如上雲）只需要改 `paths.py` 一個檔案。

```python
# src/trafficproject/paths.py
PROJECT_ROOT = Path(__file__).resolve().parents[2]
```

`parents[2]` 而非 `.parent.parent.parent`——層數一眼可辨。

### 執行方式改變

```bash
uv run python -m trafficproject.collector
```

以模組方式執行，import 路徑固定，不受工作目錄影響。

**連帶必須修改 launchd plist**——這是最容易遺漏的一步：
檔案搬走但 plist 仍指向舊路徑，collector 會直接死亡，
且 `KeepAlive` 會使其每 10 秒重試一次，產生大量錯誤 log。
（見原則 17）

### 驗證方法

搬遷後第一件事不是跑 collector，而是：

```bash
uv run python -c "from trafficproject.paths import RAW_DIR; print(RAW_DIR)"
```

**確認算出的路徑與現有資料位於同一處。**
若 `PROJECT_ROOT` 層數算錯，`mkdir(exist_ok=True)` 會靜靜建出一個空的
`src/trafficproject/raw/`，collector 開始寫入新目錄，資料一分為二——
而且不會有任何錯誤訊息。

### 附帶修正：憑證改用 .env

原本憑證寫在 plist 的 `EnvironmentVariables`，導致更換憑證必須重啟服務。

改用 `python-dotenv`：

```python
load_dotenv(PROJECT_ROOT / ".env")     # 絕對路徑，不依賴工作目錄
CLIENT_ID = os.environ["TDX_CLIENT_ID"]   # 讀取方式不變
```

`load_dotenv` 預設不覆蓋既有環境變數，因此優先序為
**既有環境變數 > .env 檔**——同一份程式碼在開發與正式環境都能運作，
無需修改。這正是「設定與程式碼分離」的實際效益。

### 附帶修正：磁碟哨兵的位置錯誤

原本的哨兵寫在模組層級：

```python
free_gb = shutil.disk_usage(".").free / 1e9
if free_gb < 5:
    log(...)          # ← log() 定義在下方，NameError
```

兩個問題：**啟動即崩潰**，且**只在啟動時檢查一次**，
之後執行一個月都不會再檢查——形同虛設。

修正後移入主迴圈，但**每 360 輪（約 30 分鐘）才檢查一次**：

> **告警本身也需要節流。** 若每 5 秒印一次警告，
> 真正的錯誤訊息會被自己的告警淹沒。

同時新增 `except OSError` 分支，將寫檔失敗與 API 失敗分開處理（見原則 15）。

---

## 四之八、資料可用性聲明與基準線

### 資料可用性

| 日期 | 狀態 | 說明 |
|---|---|---|
| 2026-08-03 | **練習資料** | 跨機器搬遷日，資料分散於 MacBook 與 Mac mini，不完整 |
| 2026-08-04 | 可用 | 磁碟事故當日，但實測中斷輕微（見下） |
| 2026-08-05 | **正式基準** | 第一個零中斷的完整日 |

> 此聲明必須留存。三個月後回頭分析時，
> 不會記得 8/3 的資料為何呈現那個形狀。

### 修正先前對 8/4 事故的評估

事故當下依 `ls | wc -l` 估算為 8,218 / 8,640 = 95.1% 完整。

轉檔後以 `snapshot_time` 精確計算：

| 城市 | 快照數 | 最大中斷 | 中斷次數(>60s) |
|---|---|---|---|
| Taipei 8/4 | 8,423 | **113 秒** | 2 |
| NewTaipei 8/4 | 8,420 | **123 秒** | 2 |
| Taipei 8/5 | 8,437 | 25 秒 | **0** |
| NewTaipei 8/5 | 8,437 | 25 秒 | **0** |

**磁碟事故並未造成長時間中斷**，實際影響遠小於當初估計。

視覺化後另有發現：8/4 圖上的數道細窄凹陷（15:00、17:40 附近）
**並非資料缺失**，而是回傳筆數短暫掉至約 1,250 後立即恢復
——屬 TDX 端的短暫異常，非本地問題。

> **精確的指標推翻了粗略的估計。**
> `snapshot_time` 讓「缺在哪、缺多久」成為可量測的數字，
> 而不只是「總共缺多少」。

### 已建立的基準線（可用於異常判斷）

| 指標 | 穩定值 | 意義 |
|---|---|---|
| `interval_median_sec` | **10.0**（四份資料全部一致）| collector 無漂移，`sleep(INTERVAL - elapsed)` 設計有效 |
| `deduplication_ratio` | 0.585 – 0.592 | 偏離即代表上游更新頻率改變 |
| `delay_p95_sec` | 24 – 27 | 端到端延遲穩定 |
| `delay_max_sec` | 604 – 608 | 每日皆有約 10 分鐘的極端延遲，屬常態 |
| 快照數/日 | 8,420 – 8,437 | 低於此值即代表有中斷 |

### 資料規模

| 日期 | Taipei | NewTaipei | 合計 |
|---|---|---|---|
| 8/4 | 985 萬 | 804 萬 | 1,789 萬 |
| 8/5 | 1,004 萬 | 815 萬 | 1,819 萬 |

**約 1,800 萬筆/日**，推估兩個月累積約 **11 億筆**。

### 待處理：速度門檻設定錯誤

| 日期 | Taipei `speed>100` | NewTaipei `speed>100` |
|---|---|---|
| 8/4 | 460 筆 / 91 車 | **3,381 筆 / 239 車** |
| 8/5 | 318 筆 / 98 車 | **3,680 筆 / 208 車** |

兩天皆穩定相差 8–10 倍，**屬結構性差異而非偶發**。

配合路線數對照（NewTaipei 539 vs Taipei 391），
推論新北包含大量國道客運路線，**100 kph 對其為正常速度**。

> **結論：速度門檻必須依路線類型分別設定，不能一刀切。**
> 若沿用單一門檻，會系統性地把新北的國道班次標記為異常
> ——又是一個系統性偏誤。

待驗證：`df[df["speed"] > 100]["route_name"].value_counts()`
確認是否集中於國道路線（1961、9010 等）。

---

---

## 四之九、靜態資料模型調查：轉乘分析的地基（2026-08-07）

### 起因

核心演算法需要「每條路線有哪些站、站序如何、座標在哪」，
因此調查 TDX 的 `StopOfRoute` 與 `Station` API。

調查過程推翻了三個原本的假設，並解決了專題最初就存在的一個缺口。

### 發現 1：join key 不是 `RouteUID`

`StopOfRoute` 的文件粒度是 **`(SubRouteUID, Direction)`**，不是 `RouteUID`。

一條 `RouteUID` 底下有多個子路線，每個子路線有去返兩個方向，
各自擁有不同的站序。**用 `RouteUID` 對不到唯一的站序。**

即時位置資料中 `sub_route_uid` 與 `direction` 皆已保留，可直接對應。

### 發現 2：三個「城市」欄位，三種意義

路線 234 的 `City` 是 `Taipei`，但前 27 站的 `LocationCityCode` 是 `NWT`。

| 欄位 | 意義 |
|---|---|
| `City` / `CityCode` | 路線的**業管機關** |
| `StopUID` 前綴 | 站牌的**業管機關**（規則：`{AuthorityCode} + {StopID}`）|
| `LocationCityCode` | 站牌的**實際地理位置** |

**混用會產生錯誤結論。** 例如按 `city` 欄位切分做地理統計，
會把大半在板橋行駛的路線全數計入台北。

**附帶結論**：collector 的 `city` 分區欄位代表「資料來源」，
不代表「車輛實際所在城市」——這點必須在分析時明確。

### 發現 3：同一根站牌有多個 `StopUID`（差點致命）

比對路線 234（Taipei）與 242（NewTaipei）的尾段：

| 站名 | 234 的 StopUID | 242 的 StopUID | GeoHash | **StationID** |
|---|---|---|---|---|
| 龍山寺(剝皮寮) | `TPE223599` | `NWT20181` | `wsqqkvff2` | **1165605675** |
| 祖師廟(康定) | `TPE33243` | `NWT20182` | `wsqqky749` | **1785** |
| 西門國小 | `TPE33244` | `NWT20183` | `wsqqkyg6h` | **1755** |
| 峨眉街口 | `TPE33245` | `NWT20184` | `wsqqkz7cd` | **1780** |

GeoHash 完全相同——**這是同一根站牌桿**，
但因分屬不同業管機關而擁有不同的 `StopUID`，**連 `StopID` 本身也不同**
（223599 vs 20181），因此無法靠去除前綴來正規化。

> **若以 `StopUID` 判斷「哪些路線經過同一站」，
> 所有跨市轉乘會全部消失。**

而跨市轉乘正是本專題最有價值的部分。

**這個錯誤不會報錯、不會產生異常值、統計結果看起來完全正常**
——只會得到「雙北幾乎沒有跨市轉乘」這個荒謬結論。

**結論：轉乘分析的 join key 必須是 `StationID`。**

### 發現 4：`StationID` 是全域唯一的，但各機關只回報自己的路線

查詢 `StationID = 1755`：

| 查詢來源 | StationUID | 座標 / 地址 / Bearing | `Stops` 內容 |
|---|---|---|---|
| Taipei | `TPE1755` | 完全相同 | **1 條**（234）|
| NewTaipei | `NWT1755` | 完全相同 | **9 條**（242、624、701、231、705、62、658、264、624綠野香坡）|

**兩邊的 `Stops` 陣列互斥，不重複。**
`StationID` 確實跨機關唯一，但每個機關只回報自身管轄的路線。

> **必須合併兩市資料，才能得到一個站位實際經過的所有路線。**

影響量化：

| | 只查 Taipei | 合併兩市 |
|---|---|---|
| 西門國小經過路線 | 1 | **10** |
| 可能的轉乘組合 | 0 | **45**（C(10,2)）|

**只查一市會使轉乘機會少算一個數量級**，且同樣是靜默錯誤。

### 發現 5：`StationID` 是單向站位——這解決了專題最初的缺口

```json
"StationAddress": "康定路46號對面(向北)",
"Bearing": "N"
```

一個 `StationID` = **一個方向的一根站牌桿**，不含對向。

> **因此「同一 StationID 的轉乘」= 原地換車，步行時間趨近於零。**

專題第一天即識別出「站內步行時間無公開資料」是最大缺口
（見〈站內步行時間 — 無公開資料的處理〉），當時的結論是只能估計並揭露誤差。

**現在對同站轉乘這一類，缺口不存在了。**

### 由此衍生：對向轉乘需自行推導

現實中大量轉乘需要過馬路，但 TDX 未提供「哪兩個 StationID 互為對向」。

可用的線索有三：

1. 座標距離約 20–50 公尺
2. `Bearing` 相反（N↔S、E↔W）
3. 站名相同或高度相似

三者聯合判斷，可信度足夠。**這是從「沒有的資料」推導出「需要的關係」**，
且推導規則完全基於可觀測欄位，可解釋、可驗證。

### 發現 6：`StopBoarding` 決定轉乘是否合法

```
-1: 可下車   0: 可上下車   1: 可上車
```

一個轉乘要成立，轉乘站必須同時滿足：

| 階段 | 條件 | 允許值 |
|---|---|---|
| A 路線下車 | 可下車 | `-1` 或 `0` |
| B 路線上車 | 可上車 | `0` 或 `1` |

**並非任兩條路線經過同一站就能轉乘。**
若 A 路線在該站僅供上車，乘客根本無法下車。

此欄位會過濾掉一部分看似可行的轉乘組合。

### 確立的資料模型

```
station (StationID)              ← 實體站位，跨機關唯一，轉乘的錨點
  └─ stops (StopUID)             ← 各業者的站牌，一路線一個
       └─ route

route_stops (SubRouteUID + Direction + VersionID)   ← 站序，到站判斷用
```

### MongoDB 選型的最終理由

先前曾評估三個候選（`collector.log`、`metrics.jsonl`、靜態路線資料），
結論是靜態資料最適合，理由不是「資料是 JSON」，而是三個結構性特徵：

**一、有序陣列是本質的**
`Stops` 的順序本身承載資訊（站序）。關聯式需拆三張表再靠 `StopSequence`
重建順序；document 直接就是正確的形狀。

**二、一對多關係普遍**
`Operators`（聯營路線多家業者）、`Stops`（一路線數十站）皆是陣列。

**三、`VersionID` 使其成為 Slowly Changing Dimension**
路線會加站、減站、改路徑。**8 月的觀測資料必須用 8 月的站序比對**，
不能用 12 月的版本。需保留多版本，關聯式做 SCD Type 2 相對繁瑣。

**被否決的候選及理由**：

| 候選 | 否決理由 |
|---|---|
| `collector.log` | 量太小（100 萬筆），且 `grep` 已足夠——**沒有解決任何實際遇到的困難** |
| `metrics.jsonl` | 確實符合 document store 特徵（schema 演進三次、巢狀欄位），但可作為附帶，非主線 |

**同時明確否決 MongoDB 作為原始層**：
JSON.gz 750 MB/天、Parquet 220 MB/天，MongoDB 存原始 JSON 預估 1–2 GB/天
（BSON 與索引開銷）。以當時僅餘 26 GB 的處境，這是致命的。
且查詢模式為「掃大量資料、只取少數欄位」，欄式儲存明顯較優。

### 最終架構中三個資料庫的分工

| 儲存 | 職責 | 選型理由 |
|---|---|---|
| Parquet / BigQuery | 大量、schema 固定的時序觀測 | 欄式壓縮、分區裁剪 |
| **MongoDB** | 巢狀、有版本演進的參考資料 | 有序陣列、SCD、schema 彈性 |
| MySQL | 算好的小結果表，供 dashboard 查詢 | 低延遲點查詢 |

**大資料進、小資料出**——每個儲存都有不可替代的理由，而非為了用滿工具。

### 待確認事項

- [ ] 合併兩市 `Station` 資料後的去重邏輯（同 `StationID` 不同 `StationUID`）
- [ ] `Station` API 是否需分頁（雙北站位數量）
- [ ] 靜態資料的抓取頻率：一天一次，但 `VersionID` 變動時須**保留舊版而非覆蓋**
- [ ] 對向站位配對規則的實作與抽樣驗證

---

---

## 四之十、每日批次排程：從手動到自動（2026-08-07）

### 動機

collector 已自動化，但轉檔與體檢仍需手動執行。
**任何需要人記得去做的事，遲早會忘記**——而漏一天轉檔，
Parquet 就會缺一天，後續分析出現破洞。

### 設計決定：一支腳本，而非兩個排程

轉檔與體檢之間有依賴——**轉檔失敗則體檢無 Parquet 可讀**。

launchd 無法表達任務間的依賴關係，因此改為單一腳本內部依序執行：

```python
def run_city(city, date):
    try:
        convert_to_parquet(city, date)
    except Exception as e:
        log(f"{city} 轉檔失敗，跳過體檢: {type(e).__name__}: {e}")
        return False          # ← 依賴中斷
    try:
        df = load_day(city, date)
        dedup, snap = health_check(df, city, date)
        plot(dedup, snap, city, date)
    except Exception as e:
        log(f"{city} 體檢失敗: {type(e).__name__}: {e}")
        return False
    return True
```

**兩個層次的失敗處理**：

| 範圍 | 行為 | 理由 |
|---|---|---|
| 城市之間 | 互不影響，一個失敗另一個照跑 | 兩市資料獨立 |
| 城市內部 | 轉檔失敗即跳過體檢 | 有真實依賴 |

> **這正是 Airflow 要解決的問題。**
> 手寫一次之後，才知道排程工具在解什麼——
> 依賴關係、重跑、可觀測性，這三件事現在都是自己扛。

### launchd 設定的關鍵差異

| 設定 | collector | daily_batch | 理由 |
|---|---|---|---|
| `KeepAlive` | ✓ | **✗** | 一次性任務，跑完就該結束；加了會無限重跑 |
| `RunAtLoad` | ✓ | **✗** | 不希望載入當下就執行 |
| `StartCalendarInterval` | ✗ | **✓ 03:00** | 深夜資料量少，不與工作時間衝突 |

**未選 00:05 的理由**：昨日資料剛結束，檔案系統可能仍在寫入。

**已知限制**：Mac 睡眠期間 launchd 不執行排程，醒來後才補跑。
Mac mini 已設為不睡眠，但這是依賴主機設定的脆弱點。

### 可觀測性設計

**一、開始與結束都要記錄**

```
=== batch start: 2026-08-06 ['Taipei', 'NewTaipei'] ===
=== batch done: 2 ok, 0 failed ===
```

沒有這兩行，就無法從 log 判斷「昨天到底跑了沒」——
這是排程最基本的要求。

**二、以離開碼回報結果**

```python
if failed:
    sys.exit(1)
```

`launchctl list` 第二欄即顯示狀態，**不必開啟 log 就知道昨天是否出事**。

**三、每個任務寫入獨立的 log 檔**

原先打算從 `collector.py` import `log`，但有兩個問題：

1. `collector.py` 在**模組層級**執行 `os.environ["TDX_CLIENT_ID"]`，
   import 當下即觸發——batch 不需要 TDX 憑證，卻會因此崩潰
2. batch 的訊息混入 `collector.log`，日後查詢收集中斷時會被干擾

改為共用的 `make_logger(name)` 工廠，各任務寫各自的檔案。

> **附帶發現：模組層級讀取設定，使該模組無法被安全 import。**
> 這是 Python 的常見反模式，設定應在 `main()` 內讀取或以函式延遲執行。

### 部署期問題：檔案沒有 pull

首次 `kickstart` 後 `launchctl list` 顯示離開碼 1，
且 `output/logs/` 中**沒有 `daily_batch.log`**
——代表程式在寫第一行 log 之前就死亡。

`batch.stderr.log` 給出答案：

```
No module named trafficproject.daily_batch
```

**根因**：MacBook 上測試通過後未 push／Mac mini 未 pull。

> **「本機測試通過」與「生產環境能跑」之間，還隔著一次完整的部署。**
> 而部署的每一步都可能遺漏：commit → push → pull → 重啟服務。

**對照價值**：先前 `exit 127` 只能靠猜測排查，這次三十秒定位。
差別僅在於是否保留 `StandardErrorPath`。
（呼應〈P8. 沒有 log 就沒有現場〉）

### 現況

| 任務 | 觸發方式 | 頻率 |
|---|---|---|
| collector | launchd `KeepAlive` | 持續，每城市 10 秒 |
| daily_batch | launchd `StartCalendarInterval` | 每日 03:00 |

**已無需人工介入的日常操作。**

### 刻意未做的事

**失敗通知。** 目前失敗僅記錄於 log 與離開碼，無主動告警。

未實作的理由：**尚未經歷過「因未察覺失敗而造成損失」的情境**，
因此不知道需要什麼形式的通知（Email？Slack？每日彙總？）。

> 貿然實作只會得到一個自己也不會看的通知管道。
> 待痛感出現再設計——這與 Parquet、schema 定義的引入時機是同一個判斷。

### 待重構

`inspect_day.py` 目前同時是**函式庫**（被 daily_batch import）
與**執行入口**（保有 `__main__`），存在兩種執行路徑，
日後可能不一致（例如修改了 `__main__` 卻未同步 batch）。

暫不處理，但 `daily_batch.py` 應視為唯一的排程入口，
`inspect_day.py` 的 `__main__` 僅供手動除錯。

---

---

## 四之十一、靜態資料擷取與規模驗證（2026-08-11）

### MongoDB 部署：GCP VM + Docker

| 項目 | 選擇 | 理由 |
|---|---|---|
| 位置 | GCP Compute Engine, asia-east1 | 單一雲端供應商，日後搬 BigQuery 在同一體系 |
| 規格 | e2-small (2 vCPU / 2GB) | 約 $14/月，$300 試用額度綽綽有餘 |
| 部署 | Docker Compose | 課程涵蓋，且設定可版控 |
| 網路 | **Tailscale，不開任何對外埠** | 無密碼 MongoDB 遭掃描勒索是常見事故 |

**未選擇 Atlas 託管的理由**：目標包含累積雲端維運經驗
（VM、防火牆、SSH 金鑰、服務帳號），託管服務會跳過這一段。

### 為可遷移性所做的三個設計

**一、compose 檔不含環境資訊**

```yaml
ports:
  - "${BIND_IP}:27017:27017"
```

VM 重建時 Tailscale IP 會變。抽成 `.env` 後，compose 檔與環境無關，可進版控。

**二、應用端只認一個連線字串**

```python
MONGO_URI = os.environ["MONGO_URI"]
```

```
# 自架:  mongodb://admin:xxx@traffic-db:27017/?authSource=admin
# Atlas: mongodb+srv://user:xxx@cluster.mongodb.net/
```

> **真正的遷移邊界是連線字串。**
> 只要不在程式中拼接 host／port／密碼，遷移就是換一行設定。
> 連線使用 Tailscale **主機名稱而非 IP**，VM 重建也不需修改。

**三、資料掛載到主機目錄**

未掛載 volume 的資料庫容器，資料隨容器刪除而消失。

### 部署期問題

| 問題 | 根因 |
|---|---|
| `invalid IP address: {BIND_IP}` | heredoc 未加單引號，`$` 被 shell 吃掉 |
| 認證失敗（VM 本機亦然） | **`MONGO_INITDB_ROOT_*` 僅在資料目錄為空時生效** |
| `nano`／`vim` 不存在 | GCP 最小化映像檔未預裝，改用 heredoc |

> **「初始化參數只在第一次生效」是所有官方資料庫映像檔的共同行為**
> （MySQL、PostgreSQL 亦同）。改了密碼卻連不上時，
> 它不會報錯，只會認證失敗，容易誤判為密碼打錯。

**安全性驗證**（不可省略）：

```bash
sudo ss -tlnp | grep 27017
# 只能出現 100.x.x.x:27017，不可有 0.0.0.0:27017
```

**Docker 直接修改 iptables，會繞過 GCP 防火牆規則。**
「我沒開防火牆所以安全」是錯誤的假設。

### 分頁：一個防護在隔天就生效

`StopOfRoute` 以 `$top=5000` 一次抓完（台北 1,221 筆），
當時加上了防護：

```python
if len(data) >= 5000:
    raise RuntimeError("可能被截斷，需要分頁")
```

**抓 `Station` 時立刻觸發**——兩市站位合計超過 5,000。

> **「今天夠用」不代表明天夠用，而明天可能就是隔天。**
> 若無此檢查，會拿到 5,000 筆就以為抓完——又是一個靜默錯誤。

**分頁的關鍵是 `$orderby`**：TDX 使用 OData，`$top` + `$skip` 配對分頁，
但**未指定排序時順序不保證**，會導致漏抓與重複，且不報錯。

```python
if len(batch) < PAGE_SIZE:    # 最後一頁，比等空陣列少一次呼叫
    break
skip += PAGE_SIZE
if skip > 100_000:            # 防 $skip 無效導致無限迴圈
    raise RuntimeError("分頁異常")
```

**驗證方式**：總筆數必須等於唯一 key 數。不相等即代表分頁出錯。

### 資料規模

| | Taipei | NewTaipei |
|---|---|---|
| 子路線×方向 | 1,221 | 1,349 |
| 站牌記錄 | 42,722 | 42,279 |
| 平均站數/路線 | 35.0 | 31.3 |
| RouteUID | 415 | 635 |
| **不重複 StationID** | **5,367** | **8,716** |

**兩支獨立 API 交叉驗證**：由 `StopOfRoute` 的 `Stops` 陣列聚合出的
StationID 數量，與 `Station` API 的回傳筆數**完全一致**（5,367 / 8,716）。
這確認了分頁完整且兩支 API 的資料一致。

### 核心數字：2,647 個共用站位

```
5,367 + 8,716 = 14,083
實際聯集         = 11,436
重疊             =  2,647   ← 兩市路線共用的站位
```

| 分類 | 數量 |
|---|---|
| 僅台北路線行經 | 2,720 |
| 僅新北路線行經 | 6,069 |
| **兩市共用** | **2,647**（約 23%）|

> **若採用 `StopUID` 作為 join key，這 2,647 個站位的跨市轉乘會全數消失。**
> 〈發現 3〉的推論在此得到量化證實。

合併後平均每站位 **5.4 條路線**（61,746 筆站牌記錄 / 11,436 站位），
兩兩組合粗估產生數十萬個轉乘組合——量級足以支撐統計分析。

### 儲存選型的量級對照

| 資料 | 規模 | 儲存 |
|---|---|---|
| 即時位置 | 1,800 萬筆/日，兩個月約 11 億筆 | Parquet |
| 靜態路線站牌 | 1.4 MB/市（gz），合計約 4 MB | **MongoDB** |

**相差五個數量級。** 這正是「不同性質的資料放不同儲存」的具體理由，
而非為了湊滿課程工具。

### 待處理

- [ ] **8 筆站位無 `Bearing`**（台北 1、新北 7）——對向配對規則依賴此欄位，
      需確認是否為轉運站等本無明確方向的站位
- [ ] 2,647 個交集站位寫入時須**合併** `Stops` 陣列而非覆蓋
      （兩市回傳的路線互斥，見〈發現 4〉）
- [ ] 原始 JSON 先落地 `raw/static/{date}/` 再轉換寫入，
      維持與即時資料相同的「原始層可重跑」原則
      ——靜態資料尤其重要，因為 `VersionID` 變動後**無法取回舊版**

---

---

## 四之十二、核心演算法：從 GPS 點到到站事件（2026-08-12）

### 方法論：先看形狀，再訂規則

沒有先寫演算法，而是**挑一台車、一條路線、一天的資料，把軌跡畫出來**。

三張圖一起看：
- 到最近站牌的距離 → 到站應呈 V 型谷底
- 站序隨時間推進 → 正常應為階梯狀單調上升
- 速度 → 停靠時應掉到 0

> **不知道資料長什麼樣就訂規則，訂出來的一定是錯的。**
> （呼應〈四之六〉：三個特徵都指向「異常」的 azimuth 65535，看了才知道是語意值）

### 從圖上直接讀到的四件事

**一、七趟班次清晰可辨** — 站序 1→37 單調上升後掉回 1，
證明 `(sub_route_uid, direction)` 的站序判斷可靠。

**二、00:00–05:30 是過夜停車** — 站序卡在 1、速度 0、距離約 30m。
若不排除，會得到「這台車在第 1 站停了 5.5 小時」的荒謬結論。

**三、距離谷底落在 20–30m，不會到 0** — GPS 誤差或站牌座標與實際停靠位置的偏移
（公車停路邊、站牌在人行道）。**門檻若設 10m 將什麼都抓不到。**

**四、班次間的長斜線是中午休息**，不是資料缺失。

### 門檻法的天花板

以 37 站 × 7 趟 = 259 個預期事件為基準：

| 門檻 | 抓到事件數 | 每站恰好 7 次的站數 |
|---|---|---|
| 30m | 175 | — |
| 50m | 206 | — |
| 75m | 232 | — |
| **100m** | **261** | **33 / 37（89%）** |

失敗集中在 4 站，且**兩種失敗模式方向相反**：

| 站序 | 次數 | 成因 |
|---|---|---|
| 17, 18 | 各 9 次（重複） | 站間距僅 156m，判定範圍（前後各 100m）重疊 |
| 11, 13 | 各 6 次（漏抓） | 過站不停 + 10 秒取樣未踩到 |

> **無法用單一門檻同時解決**：縮小門檻減少重複但加劇漏抓，放大則相反。
> 且 100m 已逼近站間距的物理極限（該路線有 7 處站間距 < 200m）。

**取樣間隔的物理限制**：門檻 30m 表示車輛需通過 60m 區間，
車速 30 kph 僅需 7.2 秒——**短於 10 秒的取樣間隔**，過站不停必然漏抓。

**決定**：接受 89%，先打通端到端。
待改進方向為「每趟每站取距離的**局部最小值**」，不需門檻、不受站間距影響。

> **先讓端到端跑通，才知道 11% 的誤差重不重要。**
> 若最終統計對此敏感，再回頭優化；屆時也才知道要改到多準。

### `duty_status` 過濾：語意正確 ≠ 準確度提升

濾除 `duty_status == 2` 後：

- ✅ 營運時段正確化為 05:28–22:46
- ✅ 排除過夜與待命的假資料
- ❌ 事件數僅由 261 降至 260，準確度**反而略降**（33→32 站）

> **兩個問題是獨立的**：`duty_status` 解決語意正確性，
> 89% 的準確度問題完全來自門檻法本身。
> 認清這點，才不會花時間調過濾規則卻發現準確度不動。

### 趟次切分

雙訊號判斷，缺一不可：

```python
seq_drop = ev["seq"].diff() < -5          # 站序大幅下降
time_gap = ev["arrival_time"].diff() > 30min
ev["trip"] = (seq_drop | time_gap).cumsum()
```

**閾值 -5 而非 -1** 的理由：站間距過近時 `near_seq` 會在 17→18→17 間抖動，
單純「下降即切」會誤判為新趟次。

實測結果完全正確，且**首班車從 seq 2 開始**（未經起點站）也被正確處理
——規則未硬性要求從 1 開始。

**附帶產出**：同一條路線，早尖峰 68 分、傍晚 64 分、深夜 48 分。
**尖峰比離峰多花 42%** ——專題要回答的現象，第一趟資料就浮現了。

### 效能：瓶頸在讀檔，不在計算

初版逐條子路線讀取 Parquet：

| 項目 | 單位耗時 | 全量推估 |
|---|---|---|
| 計算（2,833 台車） | 0.01s/台 | 28 秒 |
| **讀檔（1,027 條子路線各讀一次）** | **2.0s** | **34 分鐘** |

**讀檔佔 98%。** 改為「讀一次後分組處理」+ **只讀 8 個必要欄位**：

```
讀入 9,946,367 筆 (0.4s)          ← 22 欄減為 8 欄
營運中且去重後 5,426,716 筆
事件 971,108 筆 / 趟次 28,900 / 站位 5,309
完成，耗時 35.4s                   ← 34 分鐘 → 35 秒
```

> **這是 Parquet 欄式儲存的實際價值**，不是理論——
> 讀檔從 2.0 秒降到 0.4 秒。

**全量驗證**：每趟平均 33.6 個事件（台北平均 35 站，涵蓋 96%），
站位覆蓋 5,309 / 5,367（98.9%），且無任何子路線在 MongoDB 找不到站序。
**單車實測的規則在全量上同樣成立。**

---

## 四之十三、轉乘配對：一個被領域直覺抓到的 bug（2026-08-13）

### 問題定義的三次修正

**初版**：在站位 X，A 車到達後等多久有下一班車。

實測瑞光路（62 條路線）尖峰時段：**08:00–08:21 有 24 次到站，平均 45 秒一班。**

> **這個定義會得出「等待不到 1 分鐘」的結論——毫無價值。**
> 因為乘客不是隨便搭一班車，他要去特定目的地，**只有特定路線能到**。

**修正一**：分析單位必須是**特定的 (A 路線, B 路線) 配對**，而非「任何車」。

**修正二**：需要等待時間上限。初版無上限時的結果：

```
中位數 45 分鐘 / P90 8.4 小時 / 最大 19 小時
```

19 小時的「等待」只有一種解釋：**B 路線一天只有極少班次**。
這類稀疏路線在統計上淹沒了真正有意義的轉乘。

**修正三**：超過上限不刪除，而是計入**轉乘成功率**。

> **做法 A（丟棄超時樣本）會把「這個轉乘不可行」這件事藏起來。**
> 假設某配對 30 次機會、25 次等 5 分鐘、5 次等 40 分鐘——
> 丟棄後 P90 看起來很漂亮，卻隱瞞了 17% 轉不上的事實。

最終輸出同時包含 `wait_p50` / `wait_p90`（僅用成功樣本）
與 `success_rate`（成功數／總機會數），建議才完整。

### 分層設計：明細與聚合分離

`pair_transfers` 產出**完整明細**（不做任何過濾），
`aggregate_transfers` 才套用上限與樣本門檻。

理由：日後想把上限改為 15 或 30 分鐘，**不需重跑配對**。
與「原始層不清洗」是同一個原則。

**但有量級限制**：單一站位一天產生 30,019 筆明細，
5,309 站位 × 60 天 ≈ **95 億筆**。明細無法全量落地，
必須「邊算邊聚合」——每個站位算完即丟棄明細，只累積統計。

### 一個放大 43 倍的邊界效應

事件表中僅 **114 筆**（0.01%）屬於前一日，但配對後變成 **4,866 筆**（16%）。

原因：一筆 A 事件會與所有 B 路線各配對一次，**放大約 43 倍**。

> **明細層的少量雜訊，在配對後會被乘法放大。**
> 因此清理必須在配對之前，而非之後。

### 最隱蔽的 bug：`.values` 靜默轉 UTC

**症狀**：所有時刻正確平移 8 小時。

`pandas` 的 `.values` 會將 tz-aware 的 Timestamp **轉成 UTC 的 `datetime64`**，
時區資訊消失，**不報錯、不警告**。

```python
station_events["arrival_time"].iloc[0]        # 2026-08-11 17:03:14+08:00
station_events["arrival_time"].values[0]      # 2026-08-11T09:03:14   ← UTC
```

**為何所有既有檢查都沒抓到**：

| 檢查項目 | 結果 | 為何失效 |
|---|---|---|
| 筆數 | 30,019 | 正確 |
| 日期 `value_counts` | 僅 08-11 | 驗的是 `.dt.date`，非 `.values` |
| 每個時刻值 | 皆為合法時間 | 只是整體平移 |
| `tz_localize(None)` 單獨測試 | 時刻正確 | **測的不是實際執行的路徑** |

**最終是靠「深夜佔 66%、尖峰只佔 5%」這個直覺發現的。**

若不熟悉台北公車的營運節奏，此錯誤會一路帶進報告，
而所有數字看起來都完全正常——尖峰統計會對錯時段，結論全部失效。

> **領域知識是最後一道防線。**
> 當所有形式化的檢查都通過時，只剩「這個數字合不合常理」能救你。

**修正後驗證**：`a_time` 的小時分布與事件表形狀一致
（8 點 4,399 為最高峰、23 點僅 56），僅放大約 40 倍。

### 首個實質產出（瑞光路 station_id=1132，2026-08-11 尖峰）

| route_a | route_b | n_total | success_rate | wait_p50 | wait_p90 |
|---|---|---|---|---|---|
| TPE160287 | TPE108440 | 15 | 100% | 251s | **343s** |
| TPE157710 | TPE157709 | 10 | 100% | 295s | 415s |
| TPE10432 | TPE108440 | 9 | 89% | 171s | 479s |

**「尖峰時段在瑞光路從 TPE160287 轉 TPE108440，抓 6 分鐘就夠」**
——這正是專題第一天所定義的問題。

三個觀察：
1. 高排名配對的 `success_rate` 幾乎皆為 1.0 → **20 分鐘上限對可行轉乘而言很寬鬆**
2. `TPE108440` 在 15 筆中 9 次擔任 B → 該站主力路線，班次密集
3. `n_total` 僅 5–15 → **單日樣本不足**，這正是需累積兩個月資料的理由

---

---

## 四之十四、API 成本優化：先看帳單組成，再決定優化方向（2026-08-15）

### 起因

8 月 14 天已用 202 點，逼近原方案 200 點上限。
臨時升級至 400 點，但需確認消耗速率是否可控。

### 第一步：拆解帳單，而非直接調參數

TDX 的點數計價**分兩軸獨立計算**：

| 項目 | 基礎服務費率 | 8 月用量 | 點數 |
|---|---|---|---|
| 呼叫次數 | 1,500 次 / 1 點 | 194,199 次 | **129** |
| 資料存取量 | 150 MB / 1 點 | 11 GB | **73** |
| | | **合計** | **202** |

**呼叫次數佔 64%，是主要成本。**

> **不先拆解帳單就優化，很可能optimize了錯誤的維度。**

### 第二步：據此排除三個直覺方案

| 方案 | 效果 | 為何不採用 |
|---|---|---|
| `$select` 只取必要欄位 | 僅減少**資料量** | 對 64% 的呼叫成本零幫助；且破壞「原始層原封不動」原則 |
| `If-Modified-Since` (304) | 僅減少**傳輸量** | **304 仍計為一次呼叫**，省不到主要成本 |
| 全天拉長至 20 秒 | 呼叫降 50% | 等同來源更新頻率，取樣錯位；到站判定準確率預估由 89% 掉至 70–75% |

`If-Modified-Since` 這一項特別值得記：**它在直覺上是「最正統」的節流手段**，
但在「按呼叫次數計價」的模式下完全無效。

### 第三步：找出低價值高成本的時段

從既有資料反查：

| 時段 | 佔呼叫量 | 佔到站事件 |
|---|---|---|
| 深夜 22:00–06:00 | **33%** | **< 3%** |
| 其餘時段 | 67% | > 97% |

且轉乘統計中 `night` bucket 在聚合後**直接消失**
——深夜的 A 事件配對後多數找不到下一班，本就無分析價值。

**決定：深夜降頻至 15 秒（每城市 30 秒），其餘維持 5 秒（每城市 10 秒）。**

### 效果推估

| 方案 | 呼叫點數 | 資料點數 | 月合計 |
|---|---|---|---|
| 現況（全天 10 秒） | 277 | 156 | **433** |
| **採用：深夜 30 秒** | 224 | 126 | **350** |
| 全天 20 秒 | 139 | 78 | 217 |
| 全天 20 秒 + 深夜 60 秒 | 112 | 63 | 175 |

**350 點落在 400 點方案內，緩衝 12%。**
若日後需回到 200 點方案，則必須採用最後一列的組合，
並接受準確率下降——**這是已知的退路，不是現在要做的事。**

### 實作上的三個錯誤

初版程式碼有三個問題，其中兩個會直接導致失效：

```python
hour = datetime.now('TPE').hour                    # ① 字串非時區物件 → TypeError
interval = NIGHT if (hour >= 22 and hour < 6) else DAY   # ② and 應為 or → 恆假
```

**③ 最隱蔽：`interval` 在模組層級只計算一次。**

若 collector 於下午啟動，`interval` 將永遠是白天值，
**跑到半夜也不會切換**——降頻完全不生效，且不會有任何錯誤訊息。

> 這與先前 `makedirs` 的跨日問題是**同一類錯誤**：
> **「只在啟動時做一次」的東西，在長時間執行的程序中都必須重新檢視。**
> （原則 3 的再次應驗）

修正為每輪重新計算：

```python
def current_interval():
    hour = datetime.now(TPE).hour
    return NIGHT_INTERVAL if (hour >= 22 or hour < 6) else INTERVAL
```

### 驗證方式

**白天測不出降頻是否正確** —— 必須在時段切換後檢查：

1. 同城市相鄰檔案時間戳，白天應為 10 秒、深夜應為 30 秒
2. 隔日於 TDX 會員中心比對呼叫次數：約 16,900 → 預期 14,000（-17%）

> **檔案間隔只證明程式邏輯正確，用量數字才證明成本真的下降。**

### 方法論小結

本次優化的順序值得保留：

1. **拆解帳單組成** → 找出主要成本項
2. **據此排除無效方案** → 避免優化錯誤維度
3. **量化各時段的「成本 / 價值」比** → 找出可犧牲的部分
4. **推估效果後才動手** → 而非邊調邊看

**多數人會直接調 interval，而不會先確認帳單怎麼算的。**

---
## 四之十五、指標在量測自己的參數（2026-08-18 ~ 08-19）

### 起點：一個不隨樣本數改變的數字

`wait_p90` 中位數穩定在 1,005–1,035 秒（約 17 分鐘），把 `min_samples`
從 20 提高到 100 幾乎不動。當時的結論是「這是真實分布」。

排除小樣本假設之後，剩下的解釋只有兩個：真的如此，或者這個數字
根本不是在量等待時間。

### 兩個假設，一對一錯

**假設 A（錯）：分位數被釘在天花板。**
若 `build_marts` 的分母是 `n_total`，只要 `n_over/n_total > 10%`，
累積計數就永遠碰不到 `total × 0.9`，p90 會落在最後一個 bin。

實測：`wait_p90 == 1215`（bin 40 中點）只有 **7 列**，佔 0.002%。
分布平滑，峰在 1065。**假設 A 不成立。**

**假設 B（對）：這是條件分位數。**
分母是窗內成功數，語意是「在 20 分鐘內轉乘成功的那些人當中」的分位數。
而 `n_over / n_total` 的中位數是 **0.36**。

| 分位數的可解釋性 | 列數 | 佔比 |
|---|---|---|
| p50、p90 都有效（success_rate > 0.9） | 71,276 | 22.5% |
| p50 有效、p90 設限（0.5–0.9） | 144,223 | 45.5% |
| 連 p50 都設限（≤ 0.5） | 101,269 | **32.0%** |

**77.5% 的 `wait_p90` 不能當 p90 讀。**

### 鐵證：低成功率的分位數退化成量測窗本身

依 success_rate 分層取條件分位數的中位數：

```
success_rate  0.0-0.1  0.1-0.2  0.2-0.3  0.3-0.4  0.4-0.5  0.5-0.6
p50 中位         585      585      585      585      585      585
p90 中位        1065     1065     1065     1065     1065     1065
```

**前六層完全不動。**

理論推導：若 B 的班距 H 遠大於 20 分鐘且相對 A 隨機到達，等待時間在
[0, H] 上近似均勻；取 `wait ≤ 1200` 的條件分布後即為 [0, 1200] 上的
均勻分布，**與 H 無關**。以本專案的 bin 中點估計式代入，
理論條件 p50 = 585、p90 = 1065——與實測**完全相等**（非近似）。

`p90/p50` 實測中位 **1.800**，理論 1080/600 = 1.80。

### 量化：把窗開到 3600 秒的單日診斷

在 2026-08-12（週三，資料窗中段、遠離磁碟事故日）跑一次
`MAX_WAIT_SEC = 3600`。設計上只跑一次：3600 的直方圖**包含** 1200 的，
截掉 bin ≥ 40 即可還原，避免批次間變異。

族群 `succ12 >= 20`，共 50,882 組：

| | 中位數 |
|---|---|
| 現況條件 p90（窗 1200） | 975 秒 = 16.2 分 |
| 真值 p90（窗 3600、分母 n_total） | **1,335 秒 = 22.2 分** |
| 低估量 | **390 秒（40%）** |

低估幅度完全被 success_rate 預測：

| success_rate | 現況 p90 | 真值 p90 | 倍率 |
|---|---|---|---|
| < 0.5 | 1,065 | 3,075 | **2.90×** |
| 0.5–0.7 | 1,035 | 2,115 | 2.06× |
| 0.8–0.9 | 975 | 1,365 | 1.43× |
| **0.95–1.0** | 795 | 825 | **1.00×** |

最後一列是關鍵：**success_rate > 0.95 的配對，原本的數字完全正確**
（17.5% 的組低估量為 0）。不是全錯，是錯得與 success_rate 成反比。

另有 **8.0%** 的組連 60 分鐘窗都算不出 p90。

### 附帶修正：均勻假設只在窗內成立

窗內（0–20 分）切成四段各 5 分鐘：25.8% / 25.5% / 25.2% / 23.6%，
近乎均勻——這解釋了為何 585/1065 分毫不差。

窗外崩潰。整個小時六段：23.4% / 22.2% / 19.7% / 15.5% / 11.4% / 7.9%。
直接檢定 `sr36 / sr12` 應等於 3，實測僅 **2.27–2.41**。

因此由 success_rate 反推班距的估計式 `H ≈ 1200 / success_rate`
**偏低約 28%**，只能當下界，不能當估計值。

### 附帶發現：存活者偏差

`min_samples` 濾的是 `n_success >= 20` 而非 `n_total`：

| | sr > 0.9 | sr ≤ 0.5 |
|---|---|---|
| marts（10 天累加） | 22.3% | 32.5% |
| 單日診斷（succ ≥ 20） | 37.3% | 11.3% |

success_rate < 0.2 的配對需要 `n_total >= 101` 才存活得下來。
**最糟的轉乘被系統性剔除**，所以「低估 40%」是下界。

### 一併校正的認知

`n_days` 只有 8 不是 bug：那是資料窗內的平日天數
（night 因判斷順序在 weekend 之前，可橫跨全部 10 個日期）。
反推資料窗為 08-05 ~ 08-14，共 10 天，與 HANDOFF 記載的 14 天不符。

---

## 四之十六、專題定位的重新校準（2026-08-19）

### 問題不變，答案的形狀要變

原命題「在某站位從 A 轉 B，應該預留多少時間」預設了答案是一個數字，
因此被迫對 77.5% 的配對編造數字。

修正為兩段式：**「這個轉乘可行嗎？」→ 可行的話 →「該預留多久？」**

主指標改為 `success_rate`；分位數加上有效性門檻（p90 需 > 0.9、
p50 需 > 0.5），低於門檻明確回傳「無法估計」而非一個看似合理的數字。

### 更大的重新定位

進一步檢視發現，原始資料的價值遠大於單一應用：手上是**全雙北公車車隊、
每 10 秒一次的 GPS 軌跡**，而「到站事件」只是其中一種衍生物，
「轉乘等待」又只是該衍生物的一種應用——而且恰好是設限最嚴重的一種。

確認 SCHEMA 後盤點可行方向：

| 方向 | 所需欄位 | 判定 |
|---|---|---|
| 串聯（bunching）與班距規律性 | 事件表 | 現成可做 |
| 路段行車時間可靠度 | 事件表 + 趟次 | 現成可做，**且無設限問題** |
| 路況探針圖 | `speed`、`lat/lon`、`geohash` | 可做，需處理站停時間 |
| 延遲鏈拆解 | 四個時戳 + `operator_id` | 現成可做 |

最終定為：**公車服務可靠度——即時異常偵測與歷史基準**。

選擇理由是它讓串流與批次同時成為必要：「無法在不知道正常是什麼的
情況下判斷現在是否異常」——建立基準是歷史批次問題，偵測偏離是串流問題，
兩者必須交會。這也讓 Kafka 從「硬加的扣分項」變成有不可替代性論證的元件。

### 設限發現的正確定位

**不將右設限寫成研究貢獻**——那是存活分析的教科書內容。
正確位置是方法論選擇：初版指標經檢驗後發現在低成功率配對上退化為
量測窗的函數，故改採 success_rate 為主指標。

真正屬於本專案的貢獻是**那份資料集**，不是那個方法。

---

## 四之十七、導入 Kafka：判準、驗證、與一個 63% 的調校（2026-08-19）

### 判準：不可替代性論證

每個工具都要說得出「不用它會怎樣」，而且那個「怎樣」是真的痛。
先前判斷 Kafka 是扣分項——**在純批次架構下那個判斷是對的**。
改變的是問題，不是工具評價。

Kafka 在本架構的三個理由，第一個具領域特殊性：

1. **即時資料不可重取。** TDX 只給「現在」。處理邏輯寫錯時無法回頭重抓，
   因此原始串流必須先被持久化緩衝，處理邏輯才在其上跑（retention + replay）。
2. **扇出。** 一次攝取、多個獨立消費者，各自演進、各自失敗、互不影響。
3. **解耦。** 下游卡住不能讓上游停止輪詢——錯過的輪詢是永久空洞。

分區鍵選 `plate_numb`：有狀態的趟次切分本來就是 per-plate，
同車進同分區才能保序。實測 12 個 partition 的分布為 257–325，
無熱點。

**誠實記載**：每秒約 200 則，Kafka 的吞吐能力被大幅浪費。
選它不是為了吞吐，是為了重放與解耦。

### 本機先行驗證

在 Mac mini 上以 Docker Compose 跑 Kafka 4.x KRaft 單節點
（4.x 起 ZooKeeper 已移除）。**先本機再上雲**的理由：
listener 與 Tailscale 的組合在本機弄清楚，上 GCE 只是換一個 IP；
反過來會在網路與 IAM 上耗掉一整天。

兩組 listener 是關鍵設計：INTERNAL 給容器間、EXTERNAL 廣播 Tailscale IP。
同一份 compose 在兩台機器間搬移只需改 `.env`。

### Producer 批次調校：受控 A/B

同一批輸入同時發往兩個 topic，僅批次參數不同：

| | 磁碟 | 每則位元組 |
|---|---|---|
| `batch.size` 1 MB + `linger.ms` 50 | 92 MB | **73.7 B** |
| `batch.size` 16 KB + `linger.ms` 0 | 248 MB | 198.7 B |
| 同為 1,308,556 則 | | **−63%** |

機制：zstd 的字典以**批次**為單位建立。一批裝 2000 筆同結構 JSON，
欄位名只記一次；一批只裝 40 筆時，同樣的欄位名要在 50 個批次裡各記一次。

**已達上限**：`batch.size` 1 MB 約可裝 2,600 筆，而一次輪詢就是約 2,000 筆——
一次輪詢正好壓成一批。再調大無效，除非把 `linger.ms` 拉到 10 秒跨輪詢湊批，
但那會讓延遲爆掉。

一個設計陷阱：**librdkafka 的預設 `batch.size` 是 1 MB**，不是 Java client
的 16 KB。初版對照組「不設參數」與實驗組幾乎相同，A/B 會毫無訊號。
對照組必須明確設小。

### 分層的成本論證

```
Kafka（列式 JSON、逐筆）    73.7 B/筆   pd-balanced 約 $0.10/GB/月
原始 gzip 整批              約 34 B/筆
GCS parquet（欄式 zstd）    約 12 B/筆   GCS Standard 約 $0.02/GB/月
```

Kafka 比 parquet 貴約 30 倍（6 倍體積 × 5 倍單價），
換來的是逐筆分區、逐筆時戳與重放能力。

這回答了「為什麼同一份資料存三次」：三份職責不同——傳輸、重建、查詢。

retention 訂 14 天而非預設的 7 天：選 Kafka 的主因是重放，
事件偵測邏輯改版後要重跑歷史，7 天的窗口太緊。
實測 1.3–2.0 GB/日，14 天為 19–28 GB，仍在 30 GB 的位元組上限內。

---

## 四之十八、輪詢空洞：固定門檻的三次教訓（2026-08-19 ~ 08-20）

### 第一次：179 個假警報

以固定 30 秒為門檻掃描單日檔名，得到 180 次「空洞」。

但 collector 是白天 10 秒、深夜 30 秒——夜間每次正常輪詢只要抖動一秒
就被判為空洞。改為「超過該小時間隔中位數 × 3」後：

```
真空洞      1 次    15:35:44 → 15:57:46   1,322 秒
假警報    179 次    全部是 31 秒的夜間間隔
```

那 22 分鐘是一次計畫外重開機，損失約 **26 萬筆**位置記錄，不可回復。

### 交叉檢查暴露的第二件事

逐時檔案數：夜間 119–120（理論 120，正確），白天 351–354（理論 360，**少 2%**）。

中位間隔是 10 秒沒錯，但平均略高——典型的「做完事再 sleep 10 秒」
累積飄移：實際週期 = 10 秒 + 抓取與寫檔耗時。

**中位數正常但總量對不上**，只有交叉檢查才抓得到。

### 第三次：輪數計時在變動間隔下失準

深夜降頻後，`DISK_CHECK_EVERY = 360`（原設計「5 秒一輪 → 30 分鐘」）
在 150 秒一輪的時段變成 **15 小時**——磁碟檢查整晚停擺。

改為以經過時間判斷，不以輪數。

### 由此確立的做法

排程資訊由 **producer 自己宣告**，寫進 log 與 metrics tag：

```
[deep_night/150s] Taipei: 1633 records -> ... | kafka ok=... fail=...
```

下游只做比較，不做推導。排程之後再改幾次都不會壞。

---

## 四之十九、遷移到 GCP（2026-08-20）

### 部署

e2-medium（asia-east1-b）+ 獨立 50 GB pd-balanced 資料磁碟。
資料另掛磁碟的理由：可獨立擴容與快照，重建 VM 時資料不消失。
Kafka 不對公網開放，存取一律走 Tailscale，SSH 限縮至 IAP 範圍。

### `nofail` 掩蓋了掛載失敗

開機後 `docker ps` 正常、Kafka healthy，但 `df -h /mnt/kafka` 顯示
`/dev/root` 19G——**資料磁碟沒掛上，Kafka 正在寫開機碟**。

原因是 fstab 用了 `/dev/disk/by-id/google-kafka-data`，而 GCP 以
`--create-disk` 建立時實際的 device-name 不同。`nofail` 讓掛載失敗
不阻止開機，於是失敗變成無聲。

改用檔案系統 UUID（與裝置命名無關），並在磁碟告警中加入掛載點檢查：
比對 `/mnt/kafka` 與 `/` 的來源裝置，相同即代表沒掛上。

**`nofail` 仍然該留**（否則磁碟出問題會導致機器開不起來），
但必須配一個開機後的檢查。

### `advertised.listeners` 的兩次現身

**第一次（正面）**：Mac mini 重開機後主機名稱被改掉，但 Kafka 的
廣播位址寫的是 Tailscale IP 而非 hostname，client 完全不受影響。

**第二次（負面）**：cutover 後 collector 連 9092（INTERNAL），
bootstrap 成功、metadata 回傳 `kafka:9092`、Mac mini 解析不到，
全部逾時。改連 9094（EXTERNAL）後正常，並移除 9092 的對外 publish——
**明確的失敗好過拿到一個壞掉的位址**。

### 一天內撞到三次的同一個形狀

| 事件 | 表現 |
|---|---|
| `except Exception` 吞掉 TypeError | 三輪「以為在送，其實一則都沒出去」 |
| 缺 `error_cb` | 連線失敗靜默五分鐘（`message.timeout.ms` 耗盡前無任何訊息） |
| `nofail` | 掛載失敗，Kafka 寫錯磁碟且一切看似正常 |

三者都是**容錯設計把失敗藏起來**。

由此確立：每一層容錯，都必須配一個把失敗說出來的管道。
`traceback.format_exc()`、`error_cb`、掛載點檢查，各自對應一個。

### 穩定性實測

cutover 後連續運行：

```
2026-08-20 20:11 ~ 08-24 10:52   4 天
kafka ok = 48,503,306
kafka fail = 0
```

跨機器、經 Tailscale WireGuard 隧道、4,850 萬則、零失敗。

---

## 四之二十、深夜降頻：先驗證前提，再動手（2026-08-24）

### 起點是一個未經驗證的假設

「02:00–04:00 一定沒有公車，可以完全不抓」——這是從班表推論的，
而驗證它的資料就在手上。

以 `duty_status == 1` 計不重複車輛數（注意 parquet 存 UTC，
須先 `tz_convert("Asia/Taipei")`，否則會去看到上午 10 點的資料）：

```
01 時   24 台   末班收尾
02 時    1 台
03 時    0 台   完全無服務
04 時   23 台   首班開始
05 時  532 台   全面發車
```

**首末班的實際邊界是量出來的**，比引用班表有說服力。

### 但仍然不停止收集

三個理由，第二個最重要：

1. **省不到東西。** 沒有公車的時段回應本來就趨近空，省下的是零乘以兩小時。
2. **會失去證明「沒有」的能力。** 停掉之後，「沒有公車」與「collector 沒開」
   在資料裡長得一模一樣，永遠分不出來。
3. **破壞不變量。** 「輪詢空洞 = 0」會變成「除了計畫內視窗以外為 0」，
   每一次分析都要記得這個例外；而且一支會定時停止再啟動的程式
   多了兩個出錯的機會。

改為降頻：`deep_night` 模式每城市 5 分鐘一次，量降至十分之一，
連續性保住，真有夜車也抓得到。

### 一個順帶的驗證

`groupby` 的輸出中「3 時」整列消失。這有兩種可能且在資料裡無法區分：
真的沒車，或那小時沒有收集。必須不先過濾、改用 `crosstab` 才分得出來。

**這正是理由 2 的具體演示**，發生在做出決定的同一天。

---

## 四之二十一、Consumer A：從批次轉檔到串流封存（2026-08-24）

### 沿用與重寫的邊界

`transform.py` 整個沿用（`SCHEMA`、`FLAT_RENAME`、`NESTED_RENAME`、
`to_table`），那是驗證過兩週的資產。

`convert_to_parquet.py` 的骨架大部分不適用：

| 批次的 | 為何不能用 |
|---|---|
| `parse_snapshot_time(檔名)` | Kafka 沒有檔名 |
| temp + merge 兩階段 | 那是為了一整天塞不進記憶體；每次 flush 只有一檔 |
| 以 `{date}.parquet` 覆蓋達成冪等 | 串流需要別的機制 |
| `pd.read_json(dtype=...)` | 來源不再是檔案 |

**重寫的是執行模型，不是演算法。**

### 三個具體陷阱

**`SnapshotTime` 會變成空的。** `TIME_COLS` 需要它，但 Kafka 訊息沒有，
`snapshot_time` 整欄會是 `NaT` **且不報錯**。改以 `fetch_time` 補上
（語意等價，差幾十毫秒，需記入文件供日後串流／批次比對時參考）。

**ID 欄位會悄悄變成數字。** `pd.read_json(dtype=...)` 的強制轉字串在串流
路徑消失；`pd.DataFrame(list_of_dicts)` 會把看似數字的 `RouteID` 推斷成
int64，**前導零消失**，再轉回字串時值已不同。這會在串流／批次比對時
變成一堆對不上的記錄，且極難溯源。

**SCHEMA drift 誤報。** 串流特有欄位（`city`、`fetch_time`、`kafka_offset`…）
會被 drift 檢查判為新增欄位。**不能直接關掉**——TDX 真的變更欄位時
那是唯一的通知。正解是呼叫 `to_table` 前先移除串流特有欄位，
讓檢查看到的欄位與批次路徑完全一致。

### 正確性的核心：兩個順序

**先寫檔，後提交 offset。** 順序反了，崩潰即永久遺失；順序正確，
崩潰只造成重複——而重複可以救。因此 `enable.auto.commit = False`。

**先驗證，後刪除。** 壓實時寫完 silver 檔須讀回驗證筆數，確認無誤才刪 staging。

### 冪等性放在記錄層而非檔案層

初版設計以 offset 區間命名檔案達成冪等，但那只在單一 partition 的
consumer 成立——12 個 partition 時一次 flush 涵蓋 12 段區間。

改為：每筆記錄攜帶 `(kafka_partition, kafka_offset)`，那是該訊息在
整個 topic 內的唯一識別碼，壓實時據此去重。**檔名可以任意。**

一般形式：**輸出的識別碼由輸入決定，不由執行時刻決定**；
去重鍵來自資料本身，不依賴檔案怎麼切。

部署當天即出現真實案例：先前以 `archiver-dev` 群組跑出的 `dt=2026-08-24`
殘留檔，與補積壓推進到當天時的輸出重疊。至少一次語意的實際樣貌。

### 兩個 GCP 陷阱

**`group.id` 與 `auto.offset.reset` 的交互。**
`auto.offset.reset` 只在群組**沒有既有 offset** 時生效。沿用測試用的
`archiver-dev` 群組會從測試停下的位置繼續，14 天積壓永遠不會被封存，
且無任何錯誤訊息。

**access scope 在 IAM 之上。** 未指定 `--scopes` 建立的 VM 預設帶
`devstorage.read_only`。即使服務帳號有 `storage.objectAdmin`，
寫入仍被拒，錯誤訊息只說 403。**先查 scope，再查 IAM。**
修正需停機（`set-service-account --scopes=cloud-platform`）。

### 驗證結果

```
ID 欄位             string（未被推斷為 int64）
snapshot_time 空值   0 / 3,417
時戳                UTC
kafka_partition     12 個 partition 分布 257–325
```

分布均勻證實 `plate_numb` 作為分區鍵無熱點——這是之後事件偵測
水平擴展的前提。

---

## 四之二十二、壓實：把冪等性從檔案層移到記錄層（2026-08-24 ~ 08-25）

### 動機：串流天生會製造小檔

批次時代是「一天結束後把 8,000 個 gz 合成一個檔」。串流沒有「一天結束」
這個時刻，只能邊收邊寫：

```
每秒約 300 筆 → 20 萬筆約 11 分鐘 → 每天約 135 次 flush
每次依 (city, dt) 分組 → 每天約 270 個檔
```

60 天累積三萬多個小檔。BigQuery 光是列舉就會慢，掃描效率也差。

標準解法不是想辦法讓串流寫大檔（記憶體撐不住），而是分兩層：
串流寫 `staging/`，隔天壓實成 `silver/` 一天一檔。

### 冪等性：初版設計是錯的

最初的設計是以 **offset 區間命名檔案**（`part-p03-000123456-000234567.parquet`），
重跑產生同一個檔名、覆蓋、無重複。

**但那只在單一 partition 的 consumer 成立。**12 個 partition 時，
一次 flush 涵蓋 12 段不同的 offset 區間，檔名放不下也不具決定性。

改為把冪等性**從檔案層移到記錄層**：每筆記錄攜帶
`(kafka_partition, kafka_offset)`，那是該訊息在整個 topic 內的唯一識別碼，
壓實時據此去重。**檔名可以任意。**

去重鍵來自資料本身，就不依賴檔案怎麼切。

### 兩趟設計：記憶體是約束，不是細節

一天 984 萬列、190 MB，全部載進 pandas 會爆掉（GCE 只有 4 GB 且 Kafka 佔著）。

**第一趟**只讀 `kafka_partition` 與 `kafka_offset` 兩欄——parquet 是欄式，
27 欄裡只讀 2 欄，實際位元組約 7%。算出「哪些列要留」的布林遮罩。

**第二趟**逐檔完整讀取、套遮罩、以 `ParquetWriter` 累加寫出。
峰值記憶體只有單一檔案的量。

第一趟有個必須避開的陷阱：把 `(partition, offset)` 收進 Python `set`，
1,200 萬個 tuple 約需 **1.5 GB**。合併成單一 int64 後只要 96 MB：

```python
key = (partition.astype("int64") << 48) | offset.astype("int64")
keep = ~pd.Series(all_keys).duplicated().to_numpy()
```

partition 是 0–11，offset 每分區約四百萬，遠不到 2^48，不會碰撞。

**資料結構的選擇決定了程式跑不跑得動**，這不是最佳化，是可行性。

（順帶：pandas 的 Series 不支援 `<<` / `>>`，必須先轉 numpy 陣列。）

### 兩天各驗一半

| 日期 | 總列數 | 去重後 | 重複 | 驗證了什麼 |
|---|---|---|---|---|
| 08-21 | 9,848,791 | 9,848,791 | **0** | 乾淨資料上**不誤刪** |
| 08-24 | 11,324,587 | 8,468,584 | **2,856,003** | 有重複時**真的刪** |

一天驗證不誤刪、一天驗證會刪，兩邊都對才算通過。

去重後的 08-24（未滿一天）847 萬列，對照 08-21 完整一天的 985 萬列——
**去重後的數字合理，去重前的比完整一天還多才是異常。**

### 285 萬筆重複的根因：先懷疑改過的東西

第一直覺是補積壓時被 OOM killer 反覆砍掉（285 萬 ÷ 20 萬緩衝 ≈ 14 次）。

實測推翻：

```
NRestarts=0
journalctl | grep -c "archiver start" → 2
dmesg | grep "killed process"        → 無
```

真正的原因是**兩個 consumer group 讀了同一段訊息**。
`archiver-dev` 這個測試群組在 MacBook 上就提交過 offset，
在 GCE 啟動時不是從最新開始，而是從 10:56 繼續，追了 4.5 小時的積壓
（約 480 萬則，台北佔 55% ≈ 264 萬），跟量到的 285 萬吻合。
接著 `archiver` 群組從 `earliest` 把包含那段在內的全部再讀一次。

**系統是健康的，重複完全是測試群組的產物**，而且被去重機制正確處理。
這反而是更好的驗證案例：兩個群組獨立消費同一段資料，
下游靠記錄層的識別碼精確合併。

### 差點造成不可逆刪除的一行

```python
assert f"len(df): {len(df)} expected_rows: {expected_rows}", f"列數不符 ..."
```

`assert` 的第一個參數是**要判斷真假的運算式**，這裡放了 f-string——
非空字串永遠為真，**斷言永遠通過**。

而 `verify()` 通過的下一行就是 `fs.rm(file_list)`。
就算 silver 檔是空的、列數不符、內容全錯，staging 照樣被刪。

**驗證程式本身也需要被驗證。**一個永遠通過的斷言比沒有斷言更危險，
因為它讓你以為有防護。

### 守衛的順序決定警告有沒有意義

初版把「silver 已存在」的高嚴重度守衛放在「staging 為空」之前，
於是正常的重跑會印出：

```
⚠️ Taipei 2026-08-21 silver 已存在，但 staging 還有 0 個檔
   可能是重放或補積壓寫入。需人工確認。
```

「還有 0 個檔」自相矛盾，而且**是個例行出現的假警報**。
警告一旦例行出現就沒人會理，真正的異常會被淹沒——這才是真正的損失。

順序改成先判斷空值，兩種情況才分得開：
staging 空 + silver 有 → 已完成；staging 有 + silver 有 → 真的需要人工確認。

### 不可逆的動作只能作用在已確認的範圍

刪除用的是**第一趟固定下來的檔案清單**，不重新 glob。

若 archiver 在壓實期間又往同一個 `dt=` 寫了新檔，重新列舉會把
那些**從未被壓實過的新檔**一起刪掉——那才是真正的資料遺失。

清單外的新檔留著，下次壓實會處理（固定檔名讓重跑安全）。
這跟 archiver 的「先寫檔、後提交 offset」是同一個原則：
**只確認你真的處理完的那一段。**

### 缺席不會自己喊

台北五天壓實完成後，手動 `ls` 才發現**新北一天都沒做**。

沒有任何機制在問「該有的東西在不在」。量測清單裡的「層間漏斗」
早就寫了這一項，但一直沒實作——而它第一次該發揮作用的場合，
就是這次整整五天的缺席。

### 資料一致性：三個免費的交叉檢查

壓實完成後，兩市五天的檔案大小提供了三個彼此獨立的驗證：

```
          Taipei    NewTaipei   比值
08-20     79.8 MB    69.6 MB    0.87    週四，20:11 cutover 後約 3.8 小時
08-21    198.5 MB   174.6 MB    0.88    週五
08-22    131.1 MB   118.1 MB    0.90    週六
08-23    124.5 MB   111.8 MB    0.90    週日
08-24    210.1 MB   179.8 MB    0.86    週一
```

**橫向比值穩定在 0.86–0.90**——兩條收集線都健康。比值飄移會比絕對值
更早暴露單邊故障。

**週末同步下降**（08-22/23 約為平日的 60%）——營運車輛少，
與「深夜幾乎沒車」是同一現象的另一面。

**08-20 的部分日對得上速率**——每秒約 300 筆 × 3.8 小時 ≈ 400 萬筆 ≈ 80 MB，
與 08-21 換算的每筆約 20 B 一致。

### BigQuery：分區設計的回報

silver 掛成 hive 分區的外部表，`city=` / `dt=` 被自動識別為分區欄位。

```
帶分區條件   掃描約 200 MB（單一分區）
不帶條件     掃描 1.3 GB（全部）
```

當初把路徑寫成 `city=.../dt=...` 就是為了這一刻。

同時完成一次端到端驗證：BigQuery 查 `city="Taipei" AND dt="2026-08-21"`
的列數是 **9,848,791**，與壓實時記錄的數字完全相同。
從 Kafka 到 GCS 到 BigQuery，數字一路對得上。

### 附帶發現：SCD Type 2 記錄的是執行次數

檢查 MongoDB 時發現 `stations` 從 11,436 變成 **34,308**，正好三倍。

```
db.stations.count_documents({})               → 34,308
db.stations.count_documents({'valid_to': None}) → 11,436
```

開關邏輯是對的（舊版有正確關閉），但它**為沒有變動的記錄也開了新版本**。
若真有資料變動，各站的版本數應該參差不齊；**正好整除的三倍等於在說
「每次執行都把全部記錄重新版本化」**。

最可能的原因是比對時把每次都會變的中繼欄位（`UpdateTime` 之類）
也算進去了，於是每一筆看起來都「變了」。

目前無害（查詢都有濾 `valid_to: None`），但版本歷史記錄的是
「我執行了幾次」而不是「資料改了幾次」——之後想查「哪一天站牌變動過」
會全是雜訊。

**整齊的比例通常代表機械性的錯誤，不是真實的資料。**

---

## 四之二十三、順序約束：從結構性歧義到判定規則 v2（2026-08-26）

### 症狀：一條路線多出五倍的趟次

事件表建好後第一次探索，就撞上一個不合理的數字：

```
TPE11881 direction 0
  站牌 67   車輛 32   趟次 1,326   平均 5.5 事件/趟（全體平均 33.4）
```

鑽下去看單一車輛，trip_0~3 是垃圾（兩個事件橫跨 seq 2→29，間隔 25 秒），
trip_4 才是真的（37 個事件、seq 4→66、51 分鐘）。四段都在 05:08 前後。

站名揭露了機制——**這條路線在同一個方向內折返經過同一條走廊**：

```
seq  2  捷運石牌站(東華)
seq  4  永明派出所        seq 29  永明派出所
seq  5  榮總              seq 28  榮總
seq 30  綜合市場(捷運石牌站)
```

`station_id` 不同（483 vs 14624），實際距離不到 100 m。

所以 v1 的「每筆觀測獨立取全線最近站」在這裡**不是雜訊，是結構性歧義**：
`argmin` 沒有任何依據可以在 seq 5 和 seq 28 之間做選擇。

### 用路線比例衡量是錯的

第一個反應是掃描 MongoDB 的站序，找出「有 `|Δseq| > 5` 且距離 < 100 m 的站對」
的路線：**308 / 2,570 = 12.0%**。

但這個數字回答不了「要不要修」。它只說有多少路線**可能**受影響，
沒說實際壞掉多少資料。改成直接量損害——用不合理的步速：

```
            總步數     倒退或原地   每站<10秒   不合理佔比   每站秒數中位
Taipei      923,918      12,232      10,243      2.43%        79.0
NewTaipei   727,300      13,360      15,703      4.00%        68.0
```

中位數 79 / 68 秒是合理的，代表**資料主體健康**。但 2.43% 是「步」的比例，
一趟平均 33 個事件，若壞步均勻散佈：

```
1 - 0.9757^33 ≈ 56% 的 trip 至少含一個壞步
```

而且趟次切分用 `seq.diff() < -5`——**一個倒退步不是污染一筆，是把一整趟切成兩趟。**

新北的「太快」多於「倒退」，台北相反。這跟幾何掃描對得上：
新北的環狀線 `min_dist = 0.0`，`argmin` 會往**前**跳到對向站。

### 過濾 vs 改判定

過濾異常趟次等於系統性剔除環狀線與雙向共用廊道——**那正是班次密集、
使用者最在意的那批**。剩下的基準會很漂亮，但漂亮來自選擇偏誤，
而且分位數會**偏低**（被切碎的短趟殘骸都是短時間），
錯得非常像對的。

選改判定。順序約束的成本也不高：候選集從「全線 67 站」縮成窗內 10 站，
`argmin` 的搜尋範圍反而變小。

### 兩個設計上的關鍵

**窗要開在陣列 index，不是 `seq` 值。** `stops["seq"]` 從 MongoDB 讀來，
不保證連續。`cur_seq + 8` 和 `cur_idx + 8` 在有缺號的路線上是不同的東西。

**趟次結束需要兩個條件，只靠時間會出錯。** 車到終點站時仍持續匹配，
`last_hit` 一直更新；折返後要等滿逾時門檻才重新播種——
**那會吃掉每一趟的前 T 分鐘**。所以：

```
主要  cur_idx 已在末站，且該筆觀測已離開它  → 立刻結束並重新播種
備援  超過 20 分鐘沒有任何匹配              → 結束並重新播種
```

有了主要條件，備援才能放寬而不傷害正常趟次。

### 加了狀態之後，原本無害的髒資料會傳染

`extract_day` 新增了一行 `dropna(subset=["lat", "lon"])`。

v1 不需要它：座標為 NaN 的那筆單純過不了距離門檻就被丟掉。
v2 有狀態——`argmin` 遇到 NaN 會回傳 NaN 的位置，
那個錯誤的 `cur_idx` 會被帶給這台車後面**所有**觀測。

**同一筆髒資料，在無狀態的演算法裡是一個點，在有狀態的演算法裡是一條線。**

### 合成資料先驗，再碰真實資料

造一條 34 站的環狀路線（去程與返程走廊相距 30 m，複製 TPE11881 的幾何），
跑三圈，加不同程度的 GPS 抖動：

```
jitter      v2 趟次(應為3)    v1 推斷趟次
  0 m           3.00             3.00
 15 m           3.15            37.30
 30 m           3.35            57.25
 50 m           4.75            59.80
```

倒退步長的分佈更直接：

```
v2   {-1: 328}              全部是窗刻意容許的那一步
v1   -1 到 -33，共 33 種      對向走廊的整段跳躍
```

零抖動時兩者一致——**這確認 v2 沒有改變乾淨資料上的行為**。
抖動一上來 v1 就崩，v2 幾乎不動。

### 實測結果

08-21 台北，同一天、同一範圍：

```
                  v1         v2        變化
事件           952,409    944,681     −0.81%
趟次            28,491     26,602     −6.6%
倒退超過1站      3,627         23     −99.4%
倒退1站          7,493      9,273     +24%（窗刻意容許的容差）
每站秒數中位      76.0       76.0      不動
短趟(<10分)      11.5%       7.1%     −38%
最長趟次         348 分     206 分
```

`每站秒數中位` 兩版完全相同，是最重要的一項——**證明修掉的是雜訊，不是主體**。

TPE11881 direction 0：

```
v1   7,357 事件 / 1,326 趟 =  5.5 事件/趟
v2   5,840 事件 /    91 趟 = 64.2 事件/趟   （全線 66 站）
```

一趟 = 一次完整走完全線。5,840 ÷ 66 ≈ 88.5 次走行，與 91 趟自洽。

（先前估「應該約 260 趟」是錯的，沒把方向拆分和路線長度算進去。）

### 修好之後，原本的指標會失去偵錯能力

順序約束之下「站序倒退」在結構上被禁止，這個指標會趨近恆真——
**修復手段讓儀表失效，這不是把故障修好，是把儀表拆掉。**

所以同時建立了不受該手段保證的替代指標：**播種次數**。
播種是唯一還會做「不受限 `argmin`」的地方，也就是唯一還可能指派錯的地方。

第一版只印總數 54,959，除以趟次得 2.07，然後發現這個數字什麼都說明不了——
因為每次播種都會開一個新段，所以「播種 ÷ 趟次」恆大於 1，
它衡量的是空段比例而不是趟次碎裂。拆成三類才有診斷力：

```
初次   每個 (路線, 方向, 車輛) 的第一筆，不可避免
終點   正常換趟，應與真實趟次同量級
逾時   備援觸發，多為停在調度場、duty_status=1 但遠離站牌的車
```

外加「空段」= 開出來但一個事件都沒有的段。合成測試驗證三者之和恆等於段數。

實測 `終點 39,647 vs 趟次 27,146`，比值 1.46，顯示終點條件會連續重複觸發
（車停在末站旁的調度彎，超過 100 m 但全域最近仍是末站）。不影響資料
——那些段沒有事件——但污染了主指標。列入待辦。

---

## 四之二十四、22 天回填與基準粒度的三次修正（2026-08-26 ~ 08-27）

### 瓶頸是天數，不是演算法

事件表修好後開始設計基準。按原則 37（先看形狀，再訂規則）先量，
結果是：小時桶死了，時段桶也很薄。

```
鍵 = (路線, 方向, 起站序, 小時)     格子 761,815   n≥50: 0.3%   最大值只有 88
鍵 = (路線, 方向, 起站序, 時段)     格子 246,245   n≥50: 21.7%  中位 17
```

推算與實測吻合：六天資料下，班距 `h` 分鐘的路線在某個小時格只有 `360/h` 筆，
要 `n ≥ 50` 需要 `h ≤ 7.2` 分鐘。

中位 17 夠算中位數，**不夠算 p90——而 p90 才是「可靠度」的重點**，
中位數只是「平均要多久」。

### 回填：schema 從正典層讀，不要抄一份

Mac mini 上有 08-04 起的 22 天舊 parquet。回填的關鍵不是搬檔案，是對齊 schema：
舊檔沒有 `kafka_partition` / `kafka_offset` / `consume_time` / `pipeline_version`，
而 BigQuery 外部表對同一 prefix 下的 schema 差異很敏感。

作法是**讀一個現有的 silver 檔當範本**，而不是在回填程式裡把 27 個欄位再抄一遍。
抄一遍等於製造第二份真相，哪天 archiver 加欄位，這裡會無聲地開始產出不相容的檔案。

實測第一天就驗證了對齊乾淨：「舊檔有、正典沒有的欄位被丟棄」那行**沒有出現**，
代表舊 schema 是新正典的嚴格子集，一欄都沒掉。

`pipeline_version` 標成 `legacy-batch`。之後若某段日期的統計異常，
第一個要問的就是「這段是哪條路徑進來的」。

### 界線要從資料算，不要從記錄裡抄

08-20 兩邊都有：GCS 只有 cutover 之後，本地是完整一天。
作法不是「合併後去重」（兩千萬列的去重在筆電上很痛），
而是**以 GCS 檔的最早時間為界切開**，結構上就不重疊。

界線是程式從資料算出來的，實測：

```
合併界線：snapshot_time < 2026-08-20 07:17:59 UTC   （台北 15:18）
```

而 DEVLOG 裡記的 cutover 是 **20:11**，差了將近五小時。
原因是 collector 在做 producer 參數 A/B 測試時就已經在發，
archiver 用 `earliest` 起跑把那段積壓也收了——
**GCS 檔的起點是「生產者開始發」，不是「cutover 完成」。**

如果照記錄寫死 20:11，會有 4.9 小時重疊、約 230 萬列重複。
**自己寫的記錄也是二手資料。**

### 交叉驗證

```
08-04（週二，舊批次管線）  9,851,286
08-21（週五，Kafka 管線）  9,848,791     差 0.03%
```

兩個不同的日子、兩條完全不同的收集路徑，列數幾乎相同。

合併後的 08-20 是正常週四的 96.5%（台北）/ 95.4%（新北）——
**補完了，而且沒有被灌水**。若 A/B 測試期真的雙寫重複，這裡會超過 100%。

兩市列數比值全程 0.81–0.83，22 天沒有一天飄掉。

### 08-16：連續三次誤判

檔案大小顯示 08-16 只有正常週日的一半（兩市同步），判定為「收集中斷約半天」。
回填後看列數，改判「缺 14 小時」。兩次都錯。

事件層的每小時分佈才給出真相——**每個小時都在正常週日的 86–103%，沒有任何缺口**：

```
             08-09(週日)   08-16      08-23(週日)
讀入          6,186,991   2,540,077
可指派/讀入       54.6%      80.5%   ←
事件            673,109     621,470       92%
趟次             18,696      18,667      100%
```

關鍵是那個 80.5%。正常日的 `duty_status` 過濾 + 去重會砍掉四成五，
因為 10 秒輪詢會把同一個 `gps_time` 抓回來好幾次。08-16 只砍掉兩成，
**代表重複本來就少，也就是輪詢變稀了**。

直接量取樣間隔證實：

```
08-09   間隔中位 20   p90 20
08-23   間隔中位 20   p90 21
08-16   間隔中位 26   p90 40
```

正常日 `p50 = p90 = 20`，分佈幾乎沒有尾巴。08-16 中位只拖到 26、
p90 卻翻倍——不是均勻放慢，是**間歇性漏抓**。

（順帶發現一個免費的健康指標：**間隔的 p90 ÷ p50**，正常 1.0–1.05，08-16 是 1.54。）

而且它**不該被排除**：p90 比中位 2.21，是三個週日裡最低的。
取樣稀疏影響的是樣本數，不是分佈形狀。

### 沒用過的軸：站牌對

樣本數不只能靠天數增加。原本的鍵是 `(路線, 方向, 起站序)`，
但**同一段路常被好幾條路線共用**。改用 `(城市, 起站 station_id, 迄站 station_id)`：

```
                路段鍵        站牌對鍵     倍數
格子數          246,245       74,641       ÷3.3
n ≥ 100            5.5%        29.9%       ×5.4
n ≥  50           21.7%        46.1%       ×2.1
n 中位              17            41        ×2.4
平均共用路線數                  3.27
```

**沒有新增任何一天資料，統計效力就翻了一倍。**

代價是快慢車混在一起——但相鄰兩站之間，快車和慢車走的是同一條路，
差別只在停不停，而那個可以用 `min_speed` 分開。

### 覆蓋率要用流量加權，不是用格子數

22 天全部回填後（平日 4 天 → 16 天）：

```
              格子數    n≥100   n≥50   n<10   中位   走行覆蓋率
weekday      75,400    57.4%   72.0%   4.9%   132     98.1%
weekend      69,244    28.5%   46.2%  15.7%    42     89.0%
```

格子數只有 72% 達標，但那 28% 的稀疏格子加起來只佔 **1.9% 的實際走行**。
冷門支線本來就沒人查。**幾乎每一次真實的公車走行，都落在資料充足的格子裡。**

### 尖峰是路段的屬性，不是城市的屬性

把所有路段混在一起看，`p90/中位` 全天在 2.0–2.53 之間，
尖峰 18 點的 p25/p50/p90 相對離峰 10 點是 +15% / +13% / +21%——
**中位只慢 13%，尾巴長 21%**。塞車對「最糟的 10%」的傷害是對典型情況的 1.6 倍。

但那個混合數字有個混淆：路段長度從 200 m 到 2 km 都有，
`p90/p50 = 2.4` 裡大部分是「路段長短不同」。在**同一路段內部**比較才有意義：

```
同路段 尖峰/離峰 中位比    p10 0.83    中位 1.05    p90 1.39    23.6% 慢兩成以上
```

典型路段尖峰只慢 5%，一成路段尖峰反而比較快。**塞車是高度局部的。**

這直接否定了「統一時間分桶」——為了 23.6% 的路段把另外 76% 的樣本砍成
二十四分之一，那些路段的分位數會變得極不穩定，而它們本來不需要分桶。

而 `p10 = 0.83`（尖峰比離峰快）是真的：選的「離峰」是 10、11、13、14 點，
包含午餐時段。市場、學校、商圈周邊本來就是中午最塞。
**所以基準不該把 7–9 / 17–19 寫死成尖峰。**

### `dwell_s` 不是停靠時間

想拆解「停靠 vs 行駛」時發現：

```
停靠中位  31 秒
純行車中位 27 秒     ← sec - dwell_s
```

公車在每站「停」31 秒、站與站之間只「開」27 秒，物理上不可能。

按 `min_speed` 分帶後，`dwell_s` 其實抓得到有沒有停（真的停 60 秒 vs
時速 15 以上 8 秒，單調且分得很開）。錯的是那個減法：
判定區直徑 200 m 而站距中位約 400 m，**一個路段有將近一半在判定區內**，
減掉之後剩的只是中間空隙的時間。

結論：**基準用到站到到站的總時間，不要拆解。**

（附帶數字：32.7% 的到站事件是時速 15 以上的過站不停，47.5% 是真的停了。
車經過了，但沒載到人。）

### 分位數不可加

使用者問「A 站到 B 站要多久」時中間可能經過 10 個路段。
把 10 個 p90 加起來等於假設十段同時全部塞車，荒謬地悲觀。

平均數可加、變異數在獨立假設下可加，而且十段的和比單段更接近常態
（CLT 站在這一邊）。所以基準表同時存兩組：

```
單一路段展示   p25/p50/p75/p90     真實分佈，不做假設
多路段合成     mean、sd            Σmean、√Σsd²，再用 mean + 1.28·sd
```

### 基準表的階梯

```
主鍵  city, from_station_id, to_station_id, day_type, band
階梯  L1 (band) → L2 (day_type) → L3 (pair)，門檻 n ≥ 30，level 欄記錄落在哪層
```

兩個刻意的決定：

**階梯在建表時展開，不留給查詢端。** 表只有約 14 萬列，展開成本是零，
換來的是服務層一次點查就結束，沒有 fallback 邏輯。

**門檻不只一個。** 中位數 `n ≥ 30` 就穩，p90 要讓尾巴有十來個點，需要 `n ≥ 100`。
用單一門檻要嘛一半的 p90 不可信，要嘛白白丟掉可用的中位數。
所以階梯用 30，另外輸出 `n` 與 `p90_reliable`，把「這個數字有多可信」
跟數字本身放在一起。

**`unreliability`（p90/p50）只在 `level='band'` 時乾淨**——降級的格子把
不同時段混在一桶，p90 被時段差異撐大，看起來像不可靠、其實只是桶太粗。

---

## 四之二十五、collector 上雲：三個無聲故障（2026-08-29）

### 分類問題先講清楚

要把 Mac mini 上的東西轉成 Airflow 之前，先確認一件事：
**collector 不該進 Airflow。**

Airflow 是批次排程器：跑一個任務到結束，判定成功或失敗。
collector 是 10 秒一次的常駐輪詢。放進去只有兩種寫法，兩種都壞：
永不結束的 task 會佔住 worker slot 且永遠看不到「成功」；
每分鐘觸發一次則排程開銷比工作本身大，而且**每次觸發之間必然有空隙**。

所以「移出 Mac mini」跟「改用 Airflow」是兩件事。collector 與 archiver
搬上 GCE 維持常駐，Airflow 只接管「每天該發生一次」的批次。

### 時區：在桌上就驗掉

搬機器最可能出的錯是 `current_mode()`：Mac mini 本地時間是台北，GCE 是 UTC。
若用的是系統本地時間，台北中午會套用深夜模式的 150 秒間隔——
**資料不會中斷，只會變稀**，正是 08-16 那種最難發現的故障。

容器預設也是 UTC，所以**這個風險在 MacBook 上就能驗掉**：

```
docker run --rm trafficproject:latest date          → UTC
docker run ... python -c "print(c.current_mode())"  → ('day', 5)
```

容器是 UTC 而模式算出台北的白天，代表 `datetime.now(TPE)` 生效。

修法是讓程式明確帶時區，**不是改 VM 的時區**——那台上還有壓實的 cron 假設 UTC，
改 VM 時區會同時動到兩個東西。**環境的隱含設定不該被信任。**

### 三個「語法合法、語意恆定」的開關

要讓 collector 在 GCE 上不寫 raw，加了一個開關。過程中連續踩了三次同一種坑：

```python
if RAW_DIR:                              # Path 物件永遠 truthy
SAVE_RAW = os.getenv(...) == "1"         # .env 卻寫成一個路徑
log(f"... len(data) records ...")         # 少了大括號
```

加上壓實那次的 `assert f"…"`，這是第四次。共同特徵是
**Python 接受了，只是意思跟你想的不同**——靜態讀是讀不出來的。

真正的解法不是更小心，是**讓開關自己報到**：

```
kafka bootstrap = kafka:9092
topic = bus.position.raw
start collecting: ['Taipei', 'NewTaipei'], day=5s night=15s deep_night=150s
raw positions: OFF
```

四行涵蓋全部由設定決定的行為。**它們寫錯都不會拋例外，只會安靜地做錯事，
這四行是唯一會現形的地方。**

事後驗證：`.env` 的 `KAFKA_BOOTSTRAP` 沒改到，是靠第一行抓到的；
`SAVE_RAW_POSITIONS` 給錯值，是靠第四行抓到的。而 `topic` 那行是後來才補的，
補之前就發生了下面那次事故。

### 同一個設定，在不同網路位置有不同的正確答案

`.env` 改成 `KAFKA_BOOTSTRAP=kafka:9092` 讓容器能用 INTERNAL listener——
**同時讓 archiver 死了七個小時**，因為 archiver 跑在主機上，解不出 `kafka` 這個名字。

```
容器（docker 網路內）   kafka:9092
主機（archiver）        100.119.53.70:9094
```

兩個值都對，只是對應不同的網路位置。硬要共用一個 `.env`，注定其中一邊是錯的。

正解：**`.env` 放憑證，位址放在各自的部署描述裡**——compose 的 `environment:`
和 systemd 的 `Environment=` 各自宣告，兩邊不會踩到對方。

（`load_dotenv()` 預設不覆蓋已存在的環境變數，所以 systemd 給的值會贏。）

### bind mount 的權限歸主機管

容器以 uid 1000 跑，主機上的 `output/logs` 是 root 建的（compose 發現目錄不存在時自動建），
於是 `PermissionError`。**Dockerfile 裡的 `chown` 對 bind mount 無效**——
掛載會用主機的擁有者覆蓋容器裡那一層。

修法是 `chown -R 1000:1000 output`。**而那個動作把 archiver 的寫入權限拿掉了**，
因為 archiver 以主機使用者身分跑，寫的是同一個目錄。

最後的解法是讓容器用主機的 uid（compose 的 `user:`），
但這又暴露第三層：`paths.py` 在 **import 時**建四個目錄，
而 `/app/output` 屬於映像裡的 uid 1000，換 uid 之後建不出子目錄。

實測確認機制：非擁有者對**已存在**的目錄做 `mkdir(exist_ok=True)` 不需要父目錄寫入權，
對**不存在**的才需要。所以 Dockerfile 預建目錄就能解掉。

根因是 **`paths.py` 在 import 時動檔案系統**。單機單使用者時完全不痛，
一進到容器、跨 uid 就冒出來。同一天它幫過一次忙（`.env` 靠它的 `load_dotenv` 才讀得到）
也扯過一次後腿。列入待辦。

### 三個無聲故障

同一天，三次「該在跑的東西沒在跑，而且是人去看才發現」：

**一、Mac mini 的 collector 死了。** `launchctl list` 顯示 PID 為 `-`、
狀態 1。plist 沒有 `KeepAlive`，**程序一死就永遠不會自己回來**。

**二、`KAFKA_TOPIC` 沒清乾淨。** 測試用的 topic 已經刪除，compose 裡卻還指著它。
Mac mini 已經停掉，於是 `ok=0 fail=879`，**約一分半的資料直接消失**。

這裡有個順序上的不對稱：

```
先刪 topic、後拿掉設定  → 立刻大量失敗
先拿掉設定、後刪 topic  → 完全無害
```

同一組動作，順序決定有沒有事故。**不可逆的收尾動作要排在最後。**

**三、archiver 重啟了 2,604 次。** `Restart=always` 盡責地每 10 秒重試一次，
整整七個多小時，累積 lag 459 萬則，沒有任何人被告知。

前兩個是「壞了不會自己起來」，第三個是「壞了會一直起來，所以看起來很正常」——
**後者更危險**。`systemctl is-active` 在崩潰迴圈裡會顯示 `active`，
唯一有意義的指標是 `NRestarts` 有沒有停止增長。

### Kafka 的價值第一次真正兌現

archiver 停了七小時，**一筆資料都沒丟**。459 萬則安穩躺在 broker 裡，
修好後從 `CURRENT-OFFSET` 繼續，每分鐘消化十萬則以上。

當初選 Kafka 的論證是「即時資料不可重取」（原則見四之十七）。
那時它是紙上的理由，這次是實測：**下游死了七小時，上游繼續，資料完好。**

---

## 四之二十六、批次鏈的兩天靜默中斷，與基準表落地（2026-08-31）

### 從 GCP 主控台用眼睛發現的

`silver/positions` 停在 08-28。當時是 08-31 下午，也就是**壓實已經連續失敗兩天**。

診斷鏈很短，但每一步都排除掉一個可能：

```
crontab -l              → 30 20 * * * UTC = 台北 04:30，設定是對的
compact.cron.log mtime  → 08-31 04:30，cron 有觸發、有寫東西
compact.log     mtime   → 停在 08-29 04:35
```

**cron 有跑但應用層的 log 一個字都沒寫**，代表它死在 `make_logger` 寫出第一行之前——
不是壓實邏輯出錯，是 import 階段或 wrapper 階段。範圍一下縮到很小。

```
/bin/sh: 1: .../scripts/run-compact.sh: not found
```

### 只存在於一台機器磁碟上的檔案，等於還沒存在

`run-compact.sh` 是當初為了避開 crontab 換行問題而建的 wrapper，
**直接在 VM 上寫的，從來沒進過 git**。08-29 處理 collector 遷移時動過 git，它就消失了。

同一個狀況的還有 `kafka/docker-compose.yml`——那是唯一一份「Kafka 是怎麼設定的」記錄。

排程的一部分、線上服務的組態，如果只存在於某台機器的磁碟上和聊天視窗裡，
那台機器壞掉就等於失傳。而**消失的當下不會有任何提示**。

### 假警報比沒有警報好驗證

重建 wrapper 時順手把欠了三次的告警補上：

```bash
set -euo pipefail
trap fail ERR        # fail() 打 ntfy，跟 disk-alert.sh 同一個 topic
```

第一次執行就發出警報——但那是**假警報**：壓實本身成功了（08-30 週日，
台北 6,225,273 / 新北 5,253,674，與其他週日相差 0.6% / 1.9%），
死在 heredoc 多寫進去的一行字面 `EOF`。

假警報反而是好事：**它證明了那條通道是通的**。
真正糟糕的情況是等到出事才發現 `curl` 打錯了。

### 一次失敗的優化，以及我違反了自己的提醒

事件抽取峰值 2 GB，GCE 那台只剩 1.4 GB。想用 parquet 的 predicate pushdown
把它壓下來：

```python
df = pd.read_parquet(src, columns=NEEDED_COLS,
                     filters=[("duty_status", "==", 1)])
```

預測砍一半，實測：

```
改前   2.93 GB
改後   2.80 GB      −4.4%
```

真正的峰值不在讀取。`drop_duplicates(subset=[...])` 會產生完整副本，
那一瞬間新舊同時存在；而 `plate_numb` / `sub_route_uid` 是 object dtype，
每個字串物件約 60 bytes 開銷，541 萬列就是 650 MB。
下推只縮小了「讀進來」那一段，副本那一段完全沒動到。

寫順序約束的迴圈時我才說過「先寫直觀版本量一次，慢再說，不要預先優化」，
然後在這裡**假設瓶頸在讀取、直接動手改、改完才量**。

改動留著（4% 是正向的，順便讓漏斗的第一層改用 parquet footer 讀出，
意圖更明確），但不要當成優化過了。

真正的產出是一個確定的數字：**事件抽取峰值 2.9 GB（實測）**。
新機器的規格因此定案：Airflow 1.5–2 + 抽取 2.9 + MySQL 0.5 + OS 0.3 ≈ 5.7 GB → 8 GB。
兩城的抽取必須串列，並行會到 5.8 GB。

### 基準表落地

27 天 × 2 城全部齊備後執行 `baseline_build.sql`：

```
路段步       39,079,025
基準格子        145,050
主鍵唯一        ✓（驗證查詢回 0 列）
```

格子數從 22 天時的 144,644 只增加到 145,050——**多的五天幾乎沒有帶來新的站牌對，
全部落在既有格子裡加深樣本數**。正是想要的形狀。

階梯的效果：

```
              格子佔比    走行佔比
weekday band    84.2%      99.4%
weekend band    66.9%      96.5%
```

週末有三分之一的格子降級，但它們只承載 3.5% 的實際走行。
**用格子數看，週末的基準只有三分之二合格；用流量看，
幾乎每一次真實走行都拿得到最細粒度的基準。**

### 不可靠指數：2.4 是被污染的，1.51 才是真的

驗證時 `不可靠指數中位 = 1.51`，而我先前一路說「應該在 2 上下」。

先前量到的 `p90/中位 = 2.0–2.53` 是**把所有路段混在一起**算的。
路段長度從 200 m 到 2 km 都有，那個比值裡大部分是「路段長短不同」。
這個混淆我當時就指出過，卻在設期望值時把污染過的數字帶了進來。

現在每個格子（單一站牌對 × 單一時段）各自算 p90/p50 再取中位數，
段長異質性被完全消掉：

```
2.4    混合了「路段有長有短」
1.51   同一段路自己跟自己比    ← 真正的不可靠度
```

中位路段典型 66 秒，最糟的一成要 100 秒。

### 驗證查詢自己有 bug，靠「跟舊量測矛盾」抓到

第一版的階梯分佈查詢算出「平日 89.0% 的走行落在 band 層」，
但先前量過「98.1% 的走行落在 n≥50 的 band 格子」——**門檻從 50 放寬到 30，
覆蓋率只可能上升，不可能掉到 89%**。

兩個數字打架，先懷疑新寫的那個。原因是 `baseline_segment.n`
存的是**被選中那一層**的樣本數：

```
level = 'band'      n = 該時段格子的樣本數
level = 'daytype'   n = 整個 day_type 的樣本數    ← 大很多
```

`SUM(n)` 把不同意義的數字加在一起，分母被灌爆。改成回頭用 L1 的真實走行數
之後是 99.4% / 96.5%，跟舊量測一致。

**欄位的可加性是它的性質之一。** `n` 回答的是「支撐這一格數字的樣本有幾筆」，
刻意如此——查詢端拿到一列基準時要知道它的可信度。代價是它不可跨層加總，
而寫下這個設計的人（我）在三天後就自己踩了一次。

---

## 四之二十七、服務層落地：MySQL 與「分位數不可加」的實測代價（2026-09-01）

### 為什麼 MySQL 不需要新機器

事件抽取實測峰值 2.9 GB，那是要開第二台機器的理由。但 MySQL 不是——
基準表 145,050 列、站序 85,001 列，**整個資料集大約 20 MB**。

實測起來之後：

```
Kafka       1.09 GB
MySQL       206 MB      ← 估 400–500，實際更低
collector    41 MB      ← 估 300，實際更低
available   1.0 GB
```

兩個估計都偏高。真正把 MySQL 壓下來的是一行設定：

```
--performance-schema=OFF
```

預設開著會吃 300–400 MB，**比這個資料庫的資料本身大二十倍**。
小型部署裡它是影響最大的單一參數。

容器上限給 768m 而不是 512m：上限是隔離機制不是調校參數，
設太緊會讓 MySQL 在正常操作時被 OOM killer 挑中，
而那台上最該保護的是 Kafka。

### 同一個名字，兩台機器解到不同的東西

要從 GCE 連 MongoDB 時拿到 `traffic-db:27017 Connection refused`。
第一反應是「Mac mini 除役時把 MongoDB 一起關掉了」——**錯的**，
`traffic-db` 根本不是 Mac mini，是同專案的另一台 GCE VM，而且活著。

```
MacBook   traffic-db → Tailscale MagicDNS → 100.108.29.47 → MongoDB 在聽
GCE       traffic-db → GCP 內部 DNS      → 10.140.0.2    → 沒東西在聽
```

MongoDB 只綁在 Tailscale 介面（當初刻意不綁 `0.0.0.0`），
所以 GCE 解析到的那個位址上什麼都沒有。

**這是 `KAFKA_BOOTSTRAP` 那個問題的第二次**：同一個連線字串在網路的
不同位置有不同的正確答案。而它的症狀特別誤導——
`Connection refused` 而不是 `Name not resolved`，**看起來像服務掛了**。

暫時解法是 GCE 的 `.env` 改用 Tailscale IP。真正的解法是讓 mongod
也綁上 VPC 內部介面，兩台同 VPC 的機器不該繞 Tailscale——
那多了一個沒必要的失敗點（`tailscaled` 掛掉，事件抽取就跟著掛）。

### 佔位符第三次被照抄

修 `MONGO_URI` 時我在指令裡寫了 `mongodb://admin:xxx@100.108.29.47:27017/`，
`xxx` 被原封不動貼上去，於是從 `Connection refused` 變成 `Authentication failed`。

這是同一個坑第三次（前兩次是 `bq query` 裡的 `'...'` 和
WHERE 條件裡的 `'<挑一條>'`）。而且那次**根本不需要重打**——
原本的連線字串密碼是對的，只有主機名要換。

順帶：改含密碼的字串不要用 `sed`。密碼裡常有 `#` `|` `/`，
正好是常見的 sed 分隔字元。

### 交換是原子的，驗證是真的

匯出用「全量重載 + `RENAME TABLE`」而不是增量更新。

增量要處理「哪些格子這次沒出現」，而那正是最容易漏的情況：
某個站牌對這次沒資料，舊的那列會留著，變成**一個永遠不會更新、
看起來卻很正常的過期數字**。

```python
cur.execute(f"SELECT COUNT(*) FROM `{tmp}`")
n_dst = cur.fetchone()[0]
assert n_dst == n_src, f"{table}: 來源 {n_src:,} 寫入後 {n_dst:,}，不符"
cur.execute(f"RENAME TABLE `{table}` TO `{old}`, `{tmp}` TO `{table}`")
```

斷言的第一個參數是**比較運算式**，不是 f-string——下一行就是不可逆的交換。
壓實那次差點因為這個把 staging 刪光，這次寫的時候是照著那個教訓寫的。

另外兩個守衛：**來源 0 列直接中止**（不要用空表覆蓋線上資料）；
**未知的 `sub_route_uid` 前綴直接中止**（城市是從前綴推的，不讓程式猜——
猜錯的話那些站序會被歸到錯的城市，查詢時只會覺得「怎麼查不到」）。

### 兩個免費的交叉印證

```
route_stop  85,001 列 ÷ 2,570 個路線方向 = 每方向 33.1 站
事件表      平均每趟 33.4 個事件
```

兩條**完全獨立**的路徑：一條來自 MongoDB 的靜態站序，
一條來自 GPS 觀測推導出的到站事件。數字對得上。

另一個是 2,570 份文件對上 1,701 個 `sub_route_uid`——
一條路線的去返程是兩份文件、同一個 uid，1.51 個方向／路線，
符合「部分路線只有單向」。

### 分位數不可加：實測的代價

端到端查詢跑出來（TPE11881 方向 0，第 4 站到第 12 站，平日早尖峰，8 個路段）：

```
把 8 段的 p90 直接相加   177+135+82+75+92+56+100+151 = 868 秒
Σmean + 1.28·√Σsd²                                   = 676 秒
                                              高估   192 秒（28%）
```

在一段 9 分鐘的行程上多算了 3.2 分鐘。**直接加 p90 等於假設八段同時塞車，
那個機率是 0.1 的八次方。**

還有個反直覺的副產品：合成後的 676/544 = **1.24**，
比任何單一路段的不可靠指數（1.36–1.77）都低。
**路段越多，整體反而越可預測**——變異數相加但不等比例放大，
這是 CLT 在「和」上的直接展現，也是為什麼長程比短程好估。

當初決定「單一路段存分位數、多路段合成存 mean/sd」時，
那只是一個統計上的正確做法。這次它變成一個可以量出來的數字。

### 一個顯示問題差點被當成資料問題

查詢結果的中文站名全是 `?????`。先量再判斷：

```sql
SELECT stop_name, CHAR_LENGTH(stop_name), LENGTH(stop_name) FROM route_stop LIMIT 3;
```

位元組 = 字數 × 3，代表 UTF-8 存得好好的，是 client 沒用 utf8mb4 解碼
（容器裡沒設 `LANG`，mysql client 退回預設字元集）。
加 `--default-character-set=utf8mb4` 就好。

**「資料壞了」和「顯示壞了」的處置天差地別，而它們長得一模一樣。**

---

## 五、部署：從腳本到服務

### 動機

原本在 MacBook 上執行，闔上蓋子即中斷。
**即時資料的殘酷特性：今天沒收到的，永遠沒有了。**
這是整個專題唯一不可逆的損失，因此部署優先於分析。

### 遷移過程

```
MacBook (開發)  →  git  →  Mac mini (常駐執行)
```

| 步驟 | 工具 | 說明 |
|---|---|---|
| 程式碼傳遞 | git / GitHub | 不用 USB／AirDrop，與日後上雲同一套流程 |
| 環境重建 | `uv sync` | `uv.lock` 確保版本完全一致 |
| 憑證 | 環境變數 | 不進 git |
| 常駐執行 | launchd | 開機自啟 + 崩潰自動重啟 |

### launchd 設定的三個關鍵

```xml
<key>PATH</key>          <!-- launchd 不繼承 shell 環境，必須自己給 -->
<key>KeepAlive</key>     <!-- 程式死掉自動重啟 -->
<key>ThrottleInterval</key>  <!-- 10 秒，避免啟動即崩潰時每秒重啟打爆 API -->
```

> **自動重啟一定要配退避。** 否則程式若在啟動階段即崩潰，
> 會變成每秒重啟一次，瞬間耗盡 API 額度。

### 部署期問題

| 問題 | 根因 | 解法 |
|---|---|---|
| `Repository not found` | private repo + 未認證（GitHub 故意回 404 而非 403） | 設定 SSH key |
| launchd `exit 78` | 路徑仍為原機器的使用者名稱 | 修正全部路徑 |
| launchd 後 `invalid_client` | plist 中的環境變數未正確填入 | 修正 plist |
| `Unload failed: 5` | `load/unload` 在新版 macOS 已半廢棄 | 改用 `bootout`／`bootstrap` |

> **排錯通則：「手動能跑、服務不能跑」，答案幾乎永遠在環境差異**
> ——PATH、環境變數、工作目錄、執行身分。程式碼是同一份，不必懷疑程式碼。

### 驗證

部署完成後執行 `kill <PID>` 測試，確認 15 秒內自動重啟並取得新 PID。

> **沒驗證過的自動重啟等於沒有。**

### 已知限制

`LaunchAgents` 需使用者登入後才啟動。
已開啟自動登入作為緩解；若需完全無人值守，應改用 `LaunchDaemons`。

---

## 六、貫穿全程的原則

1. **從問題反推粒度，從粒度反推頻率，從頻率反推資料量。**
   不要從工具想起。

2. **原始資料原封不動存一份**，所有清洗從它重算。

3. **標記而非刪除。**

4. **系統性偏誤 ≫ 隨機缺漏。**
   與研究對象相關的缺漏（如「尖峰時段的資料品質較差」）
   會直接汙染結論且無法校正。

5. **偏誤處理順序：消除 → 固定化 → 事後校正。**
   校正永遠是最後手段。

6. **4xx 該吵，5xx 該安靜重試。**

7. **讓痛感出現，再選工具。**
   面試時答「因為我遇到 Y 問題」與答「課程教的」是天壤之別。
   不為了用滿課程工具而硬塞技術。

8. **不要把「更複雜」等同於「更正確」。**
   每次要加東西先問：這個複雜度是被需求逼出來的，還是我覺得比較厲害？

9. **看錯誤「類型」比看錯誤「訊息」重要。**

10. **能用參數或環境變數傳的就不要寫死。**
    判準：換個環境就要改程式碼的，都該是設定。

11. **所有結論都要能從最原始的觀測推導出來。**
    衍生欄位（業者自填的狀態碼）品質不受你控制，只能當輔助訊號。

12. **異常筆數必須配上「涉及實體數」。**
    「一台車壞很久」與「很多台車偶爾壞」的處理方式完全不同。

13. **先理解，再聚合。**
    分類方式若建立在錯誤假設上，聚合後就再也看不見真相。

14. **單一資料集看起來都正常，對照才看得出異常。**
    品質檢查應同時涵蓋所有來源，而非逐一檢視。

15. **區分「重試能解決的」與「重試不能解決的」。**
    前者安靜重試，後者必須告警。持續重試不會好的東西，
    只會產生失敗記錄而毫無進展。

16. **產出檔應該只有一個權威來源。**
    兩台機器都能寫的檔案遲早會分岔，且不該進版控。

17. **「程式碼更新了」不等於「跑著的東西更新了」。**
    部署新版本必須重啟服務。

18. **已經正常運作的東西悄悄壞掉，比跑不起來更危險。**
    前者有明確的錯誤訊息，後者只能靠主動監控發現。

19. **型別必須在資料進入系統的第一刻就固定住。**
    中間任何環節做了推斷，資訊就已遺失，之後的 schema 救不回來。

20. **「看起來像異常」與「是異常」是兩件事。**
    必須用另一個獨立欄位交叉驗證，尤其當誤判會摧毀核心分析時。

21. **驗證要選對統計量。**
    `median`／`p95` 是穩健統計量，對少數筆的改變不敏感；
    驗證要用 `max`、`sum`、`nunique` 或 hash 這類「一有變動就會變」的量。

22. **異常有兩種成因，需要兩層判斷。**
    設備持續故障 → 以實體（車輛）為單位；
    瞬時計算誤差 → 以單筆紀錄為單位。

23. **同一類錯誤出現三次以上，代表需要結構性修正。**
    逐次修補只會讓它換個形式再出現。

24. **告警本身需要節流。**
    每 5 秒印一次警告，會讓真正的錯誤被自己的告警淹沒。

25. **精確的指標會推翻粗略的估計。**
    事故當下的目測評估往往失準，應以可量測的欄位重新計算。

26. **判斷門檻必須符合資料的內在結構。**
    單一門檻套用在異質的群體上（市區公車 vs 國道客運），
    會製造出系統性偏誤。

27. **識別碼的唯一性範圍必須先確認，不能假設。**
    `StopUID` 只在單一機關內唯一，`StationID` 才是全域的。
    選錯 join key 會讓整類關係靜默消失。

28. **來源只回報自己管轄的部分。**
    查單一來源看起來完整，合併後才發現少了一個數量級。
    這是「對照才看得出異常」在關聯資料上的版本。

29. **讀懂欄位定義，往往比多寫程式有價值。**
    `StopBoarding`、`Bearing`、`StationAddress` 三個看似次要的欄位，
    分別決定了轉乘合法性、對向判斷、與步行時間估算。

30. **任何需要人記得去做的事，遲早會忘記。**
    能排程的就排程，不要依賴紀律。

31. **排程任務的最低要求：知道它跑了沒。**
    開始與結束都要記錄，並以離開碼回報成敗。

32. **「測試通過」與「生產能跑」之間隔著一次完整部署。**
    commit → push → pull → 重啟，每一步都可能遺漏。

33. **模組層級讀取設定，會讓該模組無法被安全 import。**
    設定應在 `main()` 內讀取，或以函式延遲執行。

34. **防護性檢查會比你以為的更早生效。**
    「今天夠用」不代表明天夠用——而明天可能就是隔天。

35. **兩支獨立來源的交叉驗證，勝過單一來源的自我檢查。**
    由 A 聚合出的數字若與 B 直接回傳的一致，才算真的驗證過。

36. **可遷移性的邊界是連線字串。**
    只要不在程式中拼接 host／port／密碼，換儲存就是換一行設定。

37. **不知道資料長什麼樣就訂規則，訂出來的一定是錯的。**
    先畫圖看形狀，再決定門檻與判定條件。

38. **驗證要驗「實際執行的那條路徑」，不是「你以為在執行的那條」。**
    單獨測試 `tz_localize` 通過，不代表函式內用的 `.values` 也正確。

39. **明細層的雜訊會在配對後被乘法放大。**
    清理必須在放大之前。

40. **領域知識是最後一道防線。**
    當所有形式化檢查都通過時，只剩「這數字合不合常理」能發現錯誤。

41. **過濾掉的東西本身可能就是答案。**
    丟棄超時樣本會讓 P90 變漂亮，卻隱瞞了「轉不上」的比例。

42. **優化成本前，先拆解帳單組成。**
    否則很可能optimize了佔比最小的那個維度。

43. **最正統的節流手段，未必對你的計價模式有效。**
    `If-Modified-Since` 省流量，但 304 仍計為一次呼叫。

44. **用「成本 / 價值」比找可犧牲的部分。**
    深夜佔 33% 呼叫卻不到 3% 事件——這才是該下手的地方。

45. **指標可能在量測自己的參數。**
    若一個統計量在自變數大幅變動時紋風不動，先懷疑它量的是設定值而非現象。
    條件 p50/p90 在 success_rate 0.0–0.6 之間完全不變，量到的是 `MAX_WAIT_SEC`。

46. **理論值與實測值「完全相等」比「接近」更值得警覺。**
    分毫不差通常代表你量到的是推導本身，不是資料。

47. **過濾條件會產生存活者偏差，而偏差的方向通常對你有利。**
    `min_samples` 濾在成功數上，使最糟的配對被系統性剔除——
    因此所有基於存活樣本的結論都是下界。

48. **「答不出來」必須是契約的一部分。**
    樣本不足或超出量測窗時，回傳明確的無法估計，
    而非一個看起來像答案的數字。

49. **容錯設計會把失敗藏起來，所以每一層都要配一個把失敗說出來的管道。**
    `except Exception` 需要 traceback，librdkafka 需要 `error_cb`，
    `nofail` 需要開機後的掛載檢查。三者一天內各撞一次。

50. **沒有輸出的常駐程式，閒置與當掉長得一模一樣。**
    加一行心跳，讓「正常但沒事做」與「卡住了」可區分。

51. **固定門檻會在排程改變時失效——讓上游宣告，下游只做比較。**
    預期間隔寫進 log 與 metrics tag，不讓下游重新推導排程。

52. **間隔會變動時，任何以輪數計時的東西都會失準。**
    以經過時間判斷，不以迴圈次數。

53. **中位數正常不代表總量正確。**
    輪詢間隔中位數 10 秒，但每小時少 2%——只有交叉檢查抓得到。

54. **停止收集會讓「沒有」與「沒收」永遠無法區分。**
    真的要省，降頻不要停止。

55. **先驗證前提，再依前提動手——而驗證所需的資料通常已經在手上。**

56. **輸出的識別碼由輸入決定，不由執行時刻決定。**
    去重鍵來自資料本身，冪等性才不依賴檔案怎麼切。

57. **至少一次優於剛好一次的假象：寧可重複也不要遺失。**
    重複可在下游精確消除，遺失不可回復。順序永遠是先落地、後提交。

58. **移植演算法時，變的是執行模型，不是邏輯——但執行模型才是難點。**
    `to_table` 一行未改，外圈的來源、批次策略、冪等機制、失敗處理全部重寫。

59. **預設值不是同一個預設值。**
    librdkafka 的 `batch.size` 預設 1 MB，Java client 是 16 KB。
    對照組必須明確設定，不能靠「不設就是預設」。

60. **權限有兩層，先查外層。**
    GCP 的 access scope 在 IAM 之上；scope 唯讀時，IAM 給再多角色都無效。

61. **測試環境不能污染生產狀態。**
    consumer group、輸出路徑都要隔離——共用 offset 會讓生產端
    靜默跳過一整段資料。

62. **不要把教科書內容寫成研究貢獻。**
    右設限是存活分析的基礎知識；屬於本專案的是那份資料集與那次修正，
    不是那個方法。

63. **驗證程式本身也需要被驗證。**
    `assert` 的第一個參數若寫成 f-string，斷言永遠通過——
    一個永遠通過的檢查比沒有檢查更危險，因為它讓你以為有防護。

64. **守衛的順序決定警告有沒有意義。**
    例行情況被高嚴重度的守衛攔截，警告就變成雜訊，真正的異常反而被淹沒。

65. **整齊的比例通常代表機械性的錯誤，不是真實的資料。**
    34,308 = 11,436 × 3 揭露了 SCD 每次執行都無條件開新版本。

66. **缺席不會自己喊。**
    新北整整五天沒被壓實，因為沒有任何機制在問「該有的東西在不在」。

67. **同一份輸入的橫向比值是免費的健康指標。**
    兩市檔案大小比穩定在 0.86–0.90；比值飄移比絕對值更早暴露單邊故障。

68. **先懷疑最近改過的東西，不要先懷疑最戲劇化的解釋。**
    285 萬筆重複的第一直覺是 OOM 反覆重啟；實際上只是兩個 consumer group
    讀了同一段。

69. **不可逆的動作只能作用在你已確認處理完的範圍。**
    刪除要用第一趟固定的清單，不要重新列舉——期間新增的檔案從未被處理過。

70. **記憶體是設計約束，不是實作細節。**
    1,200 萬個 tuple 的 set 要 1.5 GB，合併成單一 int64 只要 96 MB。
    資料結構的選擇決定程式跑不跑得動，這不是最佳化。

71. **結構性的錯誤不能用機率來修。**
    同方向折返的路線上，`argmin` 在兩個相距 <100 m 的站之間沒有任何依據可選。
    那不是雜訊，是缺少約束——加門檻、加平滑都沒用，要加的是順序。

72. **用「有多少可能受影響」衡量損害是錯的。**
    12% 的路線含歧義站對，這個數字回答不了要不要修。
    直接量壞掉的步（2.43% / 4.00%）才是決策依據。

73. **明細層的比例要換算成配對層才看得出嚴重性。**
    2.43% 的壞步，在一趟 33 個事件的前提下是 56% 的趟次至少含一個壞步。

74. **過濾與修正的差別，在於誰被剔除。**
    過濾異常趟次會系統性剔除環狀線——也就是班次最密集、最多人搭的那批。
    基準會變漂亮，但漂亮來自選擇偏誤。

75. **修復手段若使原本的指標恆為真，必須同時建立獨立指標。**
    順序約束讓「站序倒退」在結構上不可能發生，那個指標就失去偵錯能力。
    不補上替代指標，等於把儀表拆掉而不是把故障修好。

76. **有狀態的演算法會把點狀的髒資料放大成線狀。**
    一筆 NaN 座標，在無狀態的 `argmin` 裡只是一筆被丟掉；
    在有 `cur_idx` 的序列指派裡，它會污染那台車後面所有的觀測。

77. **合成資料能在碰真實資料之前驗證修法。**
    造一條複製問題幾何的路線，零抖動時新舊一致（證明沒改壞主體）、
    加抖動後舊的崩新的不動（證明真的修好）。兩個方向都驗才算通過。

78. **自己寫的記錄也是二手資料。**
    DEVLOG 裡記的 cutover 是 20:11，資料算出來的界線是 15:18。
    寫死記錄裡的數字會造成 230 萬列重複；從資料算就不會。

79. **量測的層級要跟你想下的結論同層。**
    「服務涵蓋度」是事件層的性質。用檔案大小或原始列數去代理它，
    會把「取樣變稀」誤判成「收集中斷」——而這兩者的處置完全相反。

80. **覆蓋率要用流量加權，不是用格子數。**
    28% 的格子樣本不足，但它們只佔 1.9% 的實際走行。
    用格子數會低估到讓人放棄一個其實堪用的設計。

81. **在換維度之前，先問還有沒有沒用過的軸。**
    站牌對取代路段鍵，讓同一段路的多條路線併在一起算，
    沒有新增任何一天資料，統計效力就翻倍。

82. **不同的統計量需要不同的樣本門檻。**
    中位數 n≥30 就穩，p90 要 n≥100。用單一門檻要嘛產出不可信的 p90，
    要嘛白白丟掉可用的中位數。

83. **分位數不可加，平均與變異數可加。**
    十個路段的 p90 相加等於假設十段同時塞車。要合成就存 mean 與 sd，
    靠 CLT 在「和」上面重建分位數。

84. **欄位名稱錯了比沒有這個欄位更危險。**
    `dwell_s` 量的是「在站牌 100 m 內停留多久」，不是停靠時間。
    名字會讓每個讀到的人做出同樣的錯誤推論。

85. **同一個設定，在不同的網路位置有不同的正確答案。**
    容器要 `kafka:9092`，主機要 `<IP>:9094`。兩個都對。
    硬要共用一個 `.env`，注定其中一邊是錯的——憑證共用，位址各自宣告。

86. **每一個由設定決定的行為，都要在啟動時說出自己是什麼。**
    位址、topic、模式、開關——寫錯都不會拋例外，只會安靜地做錯事。
    啟動 log 是它們唯一會現形的地方。

87. **條件在語法上合法、語意上恆定，是最難察覺的一類 bug。**
    `assert f"…"` 永遠通過、`if Path(...)` 永遠為真、`== "1"` 對上一個路徑永遠為假。
    四次都不報錯。解法不是更小心，是讓行為在啟動時自己報到。

88. **無限重試比不重試更危險。**
    `Restart=always` 讓 archiver 重啟 2,604 次、七小時，看起來一切正常。
    崩潰迴圈裡 `systemctl is-active` 會顯示 active；唯一有意義的是 `NRestarts`。

89. **掛載點的權限歸主機管，容器裡的 `USER` 改不了它。**
    而且修好容器的那個 `chown`，可能正好拿掉主機服務的寫入權——
    共用目錄的兩個身分要一起想。

90. **import 不該有副作用。**
    `paths.py` 在 import 時建四個目錄。單機單使用者時完全不痛，
    一進到容器、跨 uid、唯讀檔案系統就會冒出來。

91. **不可逆的收尾動作要排在最後。**
    先刪測試 topic、後改設定 → 立刻大量失敗；反過來 → 完全無害。
    同一組動作，順序決定有沒有事故。

92. **只存在於一台機器磁碟上的檔案，等於還沒存在。**
    `run-compact.sh` 是排程的一部分，卻從沒進過 git；動一次 git 它就消失，
    而且消失的當下沒有任何提示。

93. **假警報比沒有警報好驗證。**
    新加的 `trap ERR` 第一次執行就誤報，但那證明了通道是通的——
    真正糟糕的是等到出事才發現 `curl` 打錯了。

94. **先量再優化，連提醒過別人的人也會忘。**
    假設瓶頸在讀取、直接下推、預測砍一半，實測只有 4.4%。
    真正的峰值在 `drop_duplicates` 的副本與 object dtype 的字串開銷。

95. **兩個數字打架時，先懷疑新寫的那個。**
    門檻從 50 放寬到 30，覆蓋率只可能上升。新查詢算出下降，
    所以錯的是新查詢，不是資料。

96. **欄位的可加性是它的性質之一，沒寫下來就會有人拿去加。**
    `n` 存的是「被選中那一層」的樣本數，跨層 `SUM` 沒有意義——
    而寫下這個設計的人三天後就自己踩了。

97. **期望值也會被污染的數字帶偏。**
    先前的 `p90/中位 = 2.4` 混進了段長異質性。明知那個混淆，
    仍然拿它當作新指標的預期值，於是把正確的 1.51 誤判成異常。

98. **失敗發生在哪一層，可以用「哪些 log 有寫、哪些沒寫」夾出來。**
    cron 的重導向檔有新內容、應用層的 log 停在兩天前 →
    它死在 logger 初始化之前，範圍一下從整支程式縮到 import 階段。

99. **名字解析成功，不代表解析正確。**
    `traffic-db` 在 MacBook 解到 Tailscale IP、在 GCE 解到 VPC 內部 IP，
    兩邊都「成功」，但只有一邊有服務。症狀是 `Connection refused` 而不是
    `Name not resolved`，所以看起來像服務掛了。

100. **佔位符要寫成明顯不可執行的形式。**
     `xxx` 看起來像真值，`<你的密碼>` 不會。同一個坑三次，
     每次都是「指令看起來可以直接複製」造成的。

101. **改含密碼的字串不要用 `sed`。**
     密碼裡常有 `#` `|` `/`，正好是常見的分隔字元。

102. **全量重載比增量更新更難出錯。**
     增量要處理「這次沒出現的那些列」，漏掉的話它們會留在表裡，
     變成永遠不會更新、看起來卻很正常的過期數字。

103. **「資料壞了」和「顯示壞了」長得一模一樣，處置卻天差地別。**
     中文變成 `?????` 時先量 `LENGTH` 與 `CHAR_LENGTH`，
     位元組是字數三倍就只是 client 沒解對碼。

104. **小型部署裡影響最大的參數，往往不是你會去調的那個。**
     `performance-schema=OFF` 讓 MySQL 從 500 MB 降到 206 MB——
     那部分比整個資料庫的資料大二十倍。

105. **兩條獨立路徑算出同一個數字，比任何單邊檢查都有力。**
     靜態站序算出「每方向 33.1 站」，GPS 事件算出「每趟 33.4 個事件」。
     沒有共用中間結果，所以對得上就是真的對。

---

## 七、後續規劃

> 更新於 2026-08-29。事件判定 v2、22 天回填、collector 上雲皆已完成，
> 基準表的粒度已由實測定案，SQL 已寫但尚未執行。

### 已完成

```
TDX → collector（GCE, Docker, 常駐）
        └→ Kafka（GCE, KRaft, 12 分區, 14 天 retention）
                ↓ archiver（systemd, 常駐）
           staging/ 小檔
                ↓ 壓實去重（cron，時區待修）
           silver/positions/  22 天 × 2 城
                ↓ 事件抽取 v2（順序約束）
           silver/events_v2/  → BigQuery 外部表
                ↓ baseline_build.sql
           bus.baseline_segment  →  MySQL 服務層（GCE, Docker）
```

- 判定規則 v2：順序約束解掉結構性歧義，倒退步 −99.4%，主體分佈不動
- 08-04 ~ 08-19 回填 + 08-20 切界合併，平日資料 4 天 → 16 天
- collector 容器化、搬上 GCE，Mac mini 除役（launchd 已 disable + plist 移走）
- 基準粒度定案：站牌對 × 5 時段 × 平日假日，走行覆蓋率 98.1%

### 進行中

- [x] `sql/baseline_build.sql` 執行並驗證 —— 完成 2026-08-31
      39,079,025 步階 → 145,050 格；主鍵唯一；走行覆蓋 99.4% / 96.5%
      **不可靠指數中位 1.51**（中位路段 66 秒，最糟一成 100 秒）
- [x] 匯出到 MySQL —— 完成 2026-09-01
      `baseline_segment` 145,050 列 + `route_stop` 85,001 列，`RENAME TABLE` 原子交換
      端到端查詢可用：**「歷史基準」這半題完成**

### 待做

| 項目 | 難度 | 對應題目的哪一半 |
|---|---|---|
| Airflow（Docker on GCP，新機） | 中 | 運維基礎 |
| Consumer B 串流事件偵測 | **高** | 即時異常偵測的前置 |
| Consumer C 異常偵測 | 中 | **即時異常偵測** |

**Airflow 的前提是機器**。collector + archiver + Kafka 已經吃掉現有 4 GB
的一大半，Airflow（1.5–2 GB）加上事件抽取的峰值（2 GB）裝不下。
決定是**開第二台**而非升級：批次工作沒有一個需要待在 Kafka 那台
（全部走 GCS / BigQuery API），而**批次掛掉可以重跑，收集掛掉的那一小時回不來**——
不該用可重跑的工作去冒不可重跑的風險。

**collector 不進 Airflow**。它是常駐輪詢，不是批次任務；
Airflow 只接管「每天該發生一次、有開始有結束」的工作。

### 量測與運維的欠債

- [ ] **壓實 cron 的時區**：`30 4 * * *` 在 UTC 的 GCE 上是台北 12:30，應為 `30 20 * * *`
- [ ] 層間漏斗的自動檢查（現在有現成指標：取樣間隔的 p90/p50，正常 1.0–1.05）
- [ ] 壓實失敗與 collector 停擺的 ntfy 告警
- [ ] archiver 加 `StartLimitBurst`，讓「壞了」變成看得見的 failed 狀態
- [ ] 終點條件會連續重複觸發，污染播種統計（終點 39,647 vs 趟次 27,146）
- [ ] `paths.py` 在 import 時建目錄——移到真正需要的模組裡
- [ ] `convert_to_parquet.py` / `inspect_day.py` 隨 Mac mini 除役
- [ ] `load_static.py` 的 SCD 比對排除易變中繼欄位
- [ ] log 輪替（`collector.log` 每日約 1.7 萬行）
- [ ] archiver 容器化並加入 kafka 網路，消除 `.env` 的位址矛盾
- [ ] mongod 綁上 VPC 內部介面，同 VPC 的兩台機器不該繞 Tailscale
- [ ] `sql/demo_query.sql` 與 `scripts/run-compact.sh` 進 git（後者已因不在 git 而消失過一次）

### 已知的資料特性（供基準計算時參考）

- **08-16**：取樣間隔 p90/p50 = 1.54（正常 1.0–1.05），間歇性漏抓。
  事件與趟次完整（92% / 100%），分佈形狀未受影響，**不排除**。
- **08-20**：cutover 當天，本地 + Kafka 切界合併，為正常週四的 96%。
- **08-29**：collector 搬機期間有兩段短缺口（launchd 停擺、`KAFKA_TOPIC` 指錯），
  後者確定遺失約 879 筆。

### 合規事項

- [ ] 於應用服務中揭露「資料介接交通部 TDX 平臺」並加上平臺標章
- [ ] 持續監控虛擬點數消耗速率